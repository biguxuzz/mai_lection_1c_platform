# Семинар 7: Подсистемы, функциональные опции и обновление конфигурации

**Продолжительность:** 90 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель семинара
Познакомить студентов с организацией структуры конфигурации через подсистемы, механизмом функциональных опций для управления функционалом системы, а также с практическими аспектами обновления конфигурации и работы с версиями.

## Необходимые предзнания
- Основы работы с платформой 1С:Предприятие
- Понятие конфигурации и метаданных
- Работа с документами и регистрами накопления
- Основы работы с запросами

---

## План семинара (90 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины практической работы и вопросов студентов.

### Блок 1: Подсистемы (10 минут)

#### 1.1 Назначение подсистем

**Подсистемы** — это способ организации объектов конфигурации по функциональным областям.

**Основные функции подсистем:**
- Разбиение объектов по функциональным областям
- Организация интерфейсов
- Отбор объектов в конфигурации для просмотра

**Примеры использования:**
- Подсистема "Склады" — все объекты, связанные со складским учетом
- Подсистема "Сервис" — служебные объекты и настройки
- Подсистема "Закупки" — документы и справочники для закупок

**Практическое применение:**
- При создании подсистемы можно настроить отбор в конфигурации
- Просмотр только объектов, относящихся к конкретной подсистеме
- Упрощение навигации и понимания структуры конфигурации

---

### Блок 2: Функциональные опции (15 минут)

#### 2.1 Назначение функциональных опций

**Функциональные опции** — это механизм включения/выключения части функционала программы.

**Основные характеристики:**
- Позволяют управлять доступностью функций в системе
- Можно включать или выключать функционал для всей программы
- Используются для создания различных вариантов конфигурации

**Примеры использования:**
- Использование складов (включить/выключить складской учет)
- Работа с характеристиками номенклатуры
- Интеграция с внешними системами

---

#### 2.2 Хранение значений функциональных опций

**Где хранятся значения функциональных опций:**
- В константах (наиболее простой вариант)
- В регистре сведений (для сложных случаев)

**Пример настройки:**
- Функциональная опция "Использование складов"
- Хранится в константе как галочка (включена/не включена)
- При создании документа "Приход товаров по складам" делается зависимость от функциональной опции

**Практический пример:**
```1c
// Проверка функциональной опции
Если ФункциональныеОпции.ИспользованиеСкладов() Тогда
    // Логика работы со складами
КонецЕсли;
```

---

### Блок 3: Версии конфигурации (20 минут)

#### 3.1 Проблема управления версиями

**Зачем нужны версии конфигурации:**
- Контроль обновлений системы
- Выполнение действий при переходе на новую версию
- Отслеживание текущей версии в базе данных

**Проблема:**
- Версия конфигурации в метаданных (Метаданные.Версия) — это версия разработки
- Нужно хранить версию, которая установлена в текущей базе данных
- При обновлении конфигурации нужно выполнить миграционные действия

---

#### 3.2 Выбор объекта для хранения версии

**Варианты хранения версии:**

1. **Справочник** — не очень удобно, так как версия — это одна строка
2. **Документ** — не подходит для хранения версии
3. **Функциональная опция** — сама по себе не хранит, только ссылается на константу
4. **Константа** — наиболее простой вариант для одной подсистемы
5. **Регистр сведений** — удобен для систем с несколькими подсистемами

**Когда использовать регистр сведений:**
- Система состоит из нескольких подсистем (например, библиотека стандартных подсистем)
- Нужно хранить версии разных модулей отдельно
- Измерение: подсистема, значение: версия

**Когда использовать константу:**
- Простая система с одной подсистемой
- Версия системы в целом

**Практический пример:**
- Создаем константу "ВерсияКонфигурации" типа Строка(10)
- Размещаем в подсистеме "Сервис"
- Исключаем из стандартных команд интерфейса (чтобы не показывалась в форме констант)

---

#### 3.3 Работа с версией в коде

**Получение версии из метаданных:**
```1c
// Версия конфигурации (из метаданных)
ВерсияКонфигурации = Метаданные.Версия;
```

**Получение текущей версии из базы:**
```1c
// Текущая версия в базе данных
ТекущаяВерсия = Константы.ВерсияКонфигурации.Получить();
```

**Важно:** Метаданные доступны только в серверном контексте.

**Создание общего модуля для обновления:**
- Создаем общий модуль "ОбновлениеСистемы"
- Делаем его серверным
- Создаем экспортную процедуру "ПроверкаВерсии"

---

### Блок 4: События системы (10 минут)

#### 4.1 Событие "При начале работы системы"

**Назначение:**
- Выполняется при запуске системы
- Подходит для проверки версии и выполнения обновлений

**Реализация:**
```1c
Процедура ПриНачалеРаботыСистемы()
    
    ОбновлениеСистемы.ПроверкаВерсии();
    
КонецПроцедуры
```

**Важно:** 
- Событие выполняется в клиентском контексте
- Для работы с метаданными нужен вызов серверной процедуры

---

### Блок 5: Процедура обновления конфигурации (25 минут)

#### 5.1 Структура процедуры обновления

**Логика обновления:**
1. Получить версию конфигурации из метаданных
2. Получить текущую версию из базы данных
3. Сравнить версии
4. Если версии отличаются — выполнить действия обновления
5. Сохранить новую версию в константу

**Пример структуры:**
```1c
Процедура ПроверкаВерсии() Экспорт
    
    ВерсияКонфигурации = Метаданные.Версия;
    ТекущаяВерсия = Константы.ВерсияКонфигурации.Получить();
    
    Если ВерсияКонфигурации <> ТекущаяВерсия Тогда
        
        // Выполнение действий обновления в зависимости от версии
        Если ВерсияКонфигурации = "1.0.2" Тогда
            ОбновлениеНаВерсию102();
        КонецЕсли;
        
        // Сохранение новой версии
        Константы.ВерсияКонфигурации.Установить(ВерсияКонфигурации);
        
    КонецЕсли;
    
КонецПроцедуры
```

---

#### 5.2 Пример обновления: выравнивание отрицательных остатков

**Задача:** При обновлении на версию 1.0.2 автоматически оприходовать все отрицательные остатки товаров.

**Алгоритм:**
1. Собрать отрицательные остатки по складам
2. Сгруппировать их по складам
3. Для каждого склада создать документ "Приходование товаров"
4. Заполнить табличную часть документа товарами с отрицательными остатками
5. Провести документы

**Выбор модели работы с данными:**
- Объектная модель — долгий путь для получения остатков
- Запросы — оптимальный вариант для получения остатков

---

#### 5.3 Запрос для получения отрицательных остатков

**Структура запроса:**
- Регистр накопления: "ТоварыНаСкладах"
- Измерения: Номенклатура, Характеристика, Склад
- Ресурс: Количество
- Условие: Остаток < 0
- Группировка: по складам

**Особенности результата:**
- Запрос возвращает не плоскую таблицу, а дерево
- Первый уровень — склады
- Второй уровень — товары с отрицательными остатками на каждом складе

**Пример запроса:**
```1c
Запрос = Новый Запрос;
Запрос.Текст = 
"ВЫБРАТЬ
|    ТоварыНаСкладахОстатки.Склад КАК Склад,
|    ТоварыНаСкладахОстатки.Номенклатура КАК Номенклатура,
|    ТоварыНаСкладахОстатки.Характеристика КАК Характеристика,
|    ТоварыНаСкладахОстатки.КоличествоОстаток КАК Количество
|ИЗ
|    РегистрНакопления.ТоварыНаСкладах.Остатки КАК ТоварыНаСкладахОстатки
|ГДЕ
|    ТоварыНаСкладахОстатки.КоличествоОстаток < 0
|
|СГРУППИРОВАТЬ ПО
|    ТоварыНаСкладахОстатки.Склад,
|    ТоварыНаСкладахОстатки.Номенклатура,
|    ТоварыНаСкладахОстатки.Характеристика";
```

---

#### 5.4 Обработка результата запроса

**Структура обработки:**
1. Проверить, не пустой ли результат запроса
2. Если пустой — завершить процедуру
3. Если не пустой — обойти результат по группировкам (склады)
4. Для каждого склада создать документ
5. Обойти товары по складу и добавить в табличную часть

**Пример кода:**
```1c
РезультатЗапроса = Запрос.Выполнить();

Если РезультатЗапроса.Пустой() Тогда
    Возврат;
КонецЕсли;

// Обход по складам
ВыборкаПоСкладам = РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);

Пока ВыборкаПоСкладам.Следующий() Цикл
    
    // Создание документа для склада
    ДокументПриход = Документы.ПриходованиеТоваров.СоздатьДокумент();
    ДокументПриход.Дата = ТекущаяДата();
    ДокументПриход.Склад = ВыборкаПоСкладам.Склад;
    
    // Обход товаров по складу
    ВыборкаПоТоварам = ВыборкаПоСкладам.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
    
    Пока ВыборкаПоТоварам.Следующий() Цикл
        
        НоваяСтрока = ДокументПриход.Товары.Добавить();
        ЗаполнитьЗначенияСвойств(НоваяСтрока, ВыборкаПоТоварам);
        НоваяСтрока.Количество = -ВыборкаПоТоварам.Количество; // Делаем положительным
        
    КонецЦикла;
    
    ДокументПриход.Записать(РежимЗаписиДокумента.Проведение);
    
КонецЦикла;
```

**Функция ЗаполнитьЗначенияСвойств:**
- Стандартная процедура для заполнения реквизитов по одинаковым именам
- Сопоставляет структуры по именам свойств
- Упрощает заполнение данных

---

### Блок 6: Транзакции (10 минут)

#### 6.1 Необходимость транзакций

**Проблема:**
- При большом количестве отрицательных остатков документы могут долго создаваться
- Если произойдет ошибка в середине процесса, часть документов будет создана, а версия конфигурации может быть обновлена
- Нужна атомарность операции: либо все выполняется, либо ничего

**Решение:** Использование транзакций.

---

#### 6.2 Организация транзакции

**Структура транзакции:**
```1c
НачатьТранзакцию();

Попытка
    
    // Выполнение действий обновления
    // Создание документов
    // Обновление версии конфигурации
    
    ЗафиксироватьТранзакцию();
    
Исключение
    
    ОтменитьТранзакцию();
    
КонецПопытки;
```

**Принцип работы:**
- Все действия внутри транзакции выполняются атомарно
- Если произошла ошибка — все изменения откатываются
- Если все успешно — изменения фиксируются

**Практический пример:**
- Если при создании документов произойдет ошибка
- Транзакция откатится
- Версия конфигурации не обновится
- Документы не будут созданы
- Отрицательные остатки останутся без изменений

---

## Резюме семинара (5 минут)

### Ключевые моменты:

1. **Подсистемы** — организация объектов по функциональным областям
   - Упрощение навигации
   - Отбор объектов в конфигурации
   - Организация интерфейсов

2. **Функциональные опции** — управление доступностью функций
   - Хранение в константах или регистре сведений
   - Включение/выключение функционала
   - Зависимость объектов от функциональных опций

3. **Версии конфигурации** — контроль обновлений
   - Версия в метаданных (версия разработки)
   - Версия в базе данных (текущая установленная версия)
   - Хранение в константе или регистре сведений

4. **Событие "При начале работы системы"** — точка входа для обновлений
   - Выполняется при запуске системы
   - Требует вызова серверной процедуры для работы с метаданными

5. **Процедура обновления** — миграция данных при обновлении
   - Сравнение версий
   - Выполнение действий в зависимости от версии
   - Сохранение новой версии

6. **Запросы для получения остатков** — оптимальный способ работы с данными
   - Использование итогов регистров накопления
   - Получение дерева результатов при группировке
   - Обход результатов по группировкам

7. **Транзакции** — обеспечение атомарности операций
   - Либо все выполняется, либо ничего
   - Защита от частичного выполнения обновления
   - Откат изменений при ошибках

---

### Практические рекомендации:

1. **Организация структуры:**
   - Используйте подсистемы для логической группировки объектов
   - Размещайте служебные объекты в подсистеме "Сервис"

2. **Функциональные опции:**
   - Используйте для создания различных вариантов конфигурации
   - Храните значения в константах для простых случаев
   - Используйте регистр сведений для сложных систем

3. **Управление версиями:**
   - Всегда храните текущую версию в базе данных
   - Выполняйте миграционные действия при обновлении
   - Используйте транзакции для атомарности

4. **Работа с данными:**
   - Используйте запросы для получения остатков и оборотов
   - Помните о структуре результата (дерево при группировке)
   - Используйте функцию ЗаполнитьЗначенияСвойств для упрощения кода

5. **Транзакции:**
   - Всегда используйте транзакции при обновлении конфигурации
   - Обеспечивайте атомарность операций
   - Правильно обрабатывайте исключения

---

### Вопросы для самопроверки:

1. Для чего используются подсистемы в конфигурации 1С?
2. Где хранятся значения функциональных опций?
3. В чем разница между версией конфигурации в метаданных и версией в базе данных?
4. Где лучше хранить версию конфигурации: в константе или в регистре сведений?
5. В каком контексте доступны метаданные?
6. Какое событие используется для проверки версии при запуске системы?
7. Почему для получения остатков лучше использовать запросы, а не объектную модель?
8. Что возвращает запрос с группировкой по измерениям регистра накопления?
9. Зачем нужны транзакции при обновлении конфигурации?
10. Что произойдет, если в транзакции произойдет ошибка?

---

## Практическое задание:

1. Создайте подсистему "Склады" и разместите в ней соответствующие объекты
2. Создайте функциональную опцию "Использование складов" с хранением в константе
3. Создайте константу "ВерсияКонфигурации" в подсистеме "Сервис"
4. Реализуйте процедуру обновления, которая при переходе на версию 1.0.2 выравнивает отрицательные остатки
5. Организуйте транзакцию для обеспечения атомарности операции обновления

---

**До встречи на следующем семинаре! Вопросы?** 🎓

