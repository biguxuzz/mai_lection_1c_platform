# LightRAG WebUI Service

Веб-интерфейс для работы с LightRAG - официальный UI от разработчиков LightRAG.

## Описание

LightRAG WebUI предоставляет удобный веб-интерфейс для:
- Индексации документов
- Выполнения поисковых запросов
- Визуализации графа знаний
- Управления базой данных LightRAG
- Настройки параметров поиска

## Архитектура

```
┌─────────────────┐
│  Browser        │
│  (localhost)    │
└────────┬────────┘
         │ HTTP
         ↓
┌─────────────────┐
│  LightRAG       │  Port: 9621
│  WebUI          │  (Web Interface)
└────────┬────────┘
         │ API
         ↓
┌─────────────────┐      ┌──────────────┐
│  LightRAG       │ ←──→ │  LM Studio   │
│  Backend        │      │  (1234)      │
└────────┬────────┘      └──────────────┘
         │
         ↓
┌─────────────────┐
│  Graph Store    │
│  (File System)  │
└─────────────────┘
```

## Особенности

- ✅ Современный веб-интерфейс (React + Vite)
- ✅ REST API для интеграции
- ✅ Визуализация графа знаний
- ✅ Поддержка 4 режимов поиска (local, global, hybrid, naive)
- ✅ Управление документами
- ✅ Настройка параметров LLM и эмбеддингов
- ✅ Экспорт/импорт данных

## Установка

### Через Docker (рекомендуется)

```bash
# Запуск WebUI вместе с остальными сервисами
docker-compose up -d lightrag-webui

# Только WebUI
docker-compose up -d lightrag-webui
```

### Локальная установка

```bash
# Клонирование репозитория
git clone https://github.com/HKUDS/LightRAG.git
cd LightRAG

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установка с API поддержкой
pip install -e ".[api]"

# Сборка фронтенда
cd lightrag_webui
npm install
npm run build
cd ..

# Запуск сервера
python -m lightrag.api --host 0.0.0.0 --port 9621
```

## Использование

### Доступ к WebUI

Откройте в браузере:
```
http://localhost:9621
```

### Основные функции

#### 1. Индексация документов

1. Откройте вкладку **"Documents"**
2. Нажмите **"Upload"**
3. Выберите файлы или вставьте текст
4. Нажмите **"Index"**

#### 2. Поиск

1. Откройте вкладку **"Query"**
2. Введите запрос в поисковую строку
3. Выберите режим поиска:
   - **local** - локальный контекст
   - **global** - глобальный поиск
   - **hybrid** - комбинированный (рекомендуется)
   - **naive** - простой векторный
4. Нажмите **"Search"**

#### 3. Визуализация графа

1. Откройте вкладку **"Graph"**
2. Просмотрите граф знаний
3. Взаимодействуйте с узлами и связями
4. Фильтруйте по типам сущностей

#### 4. Настройки

1. Откройте вкладку **"Settings"**
2. Настройте:
   - URL LM Studio
   - Модель LLM
   - Модель эмбеддингов
   - Параметры поиска
3. Сохраните изменения

## API Endpoints

### Базовый URL
```
http://localhost:9621
```

### Документация API
```
http://localhost:9621/docs
```

### Основные endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/` | GET | Главная страница WebUI |
| `/api/health` | GET | Проверка статуса |
| `/api/insert` | POST | Индексация документа |
| `/api/query` | POST | Поиск |
| `/api/graph` | GET | Получение графа |
| `/api/documents` | GET | Список документов |
| `/api/documents/{id}` | DELETE | Удаление документа |

### Примеры запросов

#### Индексация через API

```bash
curl -X POST http://localhost:9621/api/insert \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Текст документа для индексации"
  }'
```

#### Поиск через API

```bash
curl -X POST http://localhost:9621/api/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Поисковый запрос",
    "mode": "hybrid"
  }'
```

## Конфигурация

### Переменные окружения

```yaml
# В docker-compose.yml
environment:
  - LIGHTRAG_API_URL=http://host.docker.internal:1234/v1
  - LIGHTRAG_API_KEY=lm-studio
  - LLM_MODEL=Qwen2.5-1.5B-Instruct
  - EMBEDDING_MODEL=text-embedding-nomic-embed-text-v1.5
```

### Параметры запуска

```bash
# Изменить порт
python -m lightrag.api --host 0.0.0.0 --port 9621

# Указать рабочую директорию
python -m lightrag.api --working-dir /path/to/data

# С отладкой
python -m lightrag.api --debug
```

## Интеграция

### С LightRAG Service (порт 8002)

WebUI работает независимо от нашего custom LightRAG Service:
- **WebUI** (9621) - официальный интерфейс
- **LightRAG Service** (8002) - наш REST API

Оба могут работать параллельно с общим LM Studio.

### С другими сервисами

```python
import httpx

# Индексация через WebUI API
async with httpx.AsyncClient() as client:
    # Через наш сервис (8002)
    await client.post(
        "http://localhost:8002/insert",
        json={"text": "Документ для нашего API"}
    )
    
    # Через WebUI (9621)
    await client.post(
        "http://localhost:9621/api/insert",
        json={"text": "Документ для WebUI"}
    )
```

## Структура проекта

```
lightrag-webui-service/
├── Dockerfile          # Multi-stage сборка
└── README.md          # Эта документация

Внутри контейнера:
/app/lightrag/
├── lightrag/          # Python пакет
│   ├── api.py        # API сервер
│   └── ...
├── lightrag_webui/    # Фронтенд
│   ├── dist/         # Собранные файлы
│   └── ...
└── /app/data/        # Хранилище данных
```

## Технологии

### Backend
- Python 3.12
- FastAPI
- LightRAG library

### Frontend
- React
- Vite
- TypeScript
- Bun (build tool)

## Troubleshooting

### Ошибка: IndexError в nano_vectordb

**Проблема**: 
```
IndexError: index 0 is out of bounds for axis 0 with size 0
Failed to extract document
```

**Причина**: Поврежденная или неправильно инициализированная база данных `nano_vectordb`.

**Решение:**

1. **Очистка поврежденной базы данных** (рекомендуется):
```bash
# Остановка контейнера
docker stop lection6_lightrag_webui

# Удаление volume с данными
docker volume rm lection6_lightrag_webui_data

# Перезапуск контейнера
docker-compose up -d lightrag-webui
```

2. **Использование скрипта исправления**:
```bash
# В директории проекта
chmod +x lightrag-webui-service/fix_db.sh
./lightrag-webui-service/fix_db.sh
```

3. **Проверка подключения к LMStudio**:
   - Убедитесь, что LMStudio запущен
   - Проверьте, что модель эмбеддингов загружена
   - Проверьте URL в переменных окружения (`EMBEDDING_BINDING_HOST`)

4. **Проверка логов**:
```bash
docker logs lection6_lightrag_webui
```

Скрипт `start.py` теперь автоматически проверяет подключение к LMStudio и предупреждает о проблемах с базой данных.

### WebUI не открывается

**Проблема**: Страница не загружается

**Решение:**
```bash
# Проверьте логи
docker logs lection6_lightrag_webui

# Проверьте статус
curl http://localhost:9621/

# Перезапустите
docker-compose restart lightrag-webui
```

### Ошибка подключения к LM Studio

**Проблема**: WebUI не может подключиться к LM Studio

**Решение:**
1. Убедитесь что LM Studio запущен
2. Проверьте настройки CORS в LM Studio
3. Проверьте URL в настройках WebUI
4. Убедитесь, что модель эмбеддингов загружена и доступна

### Медленная индексация

**Проблема**: Документы индексируются долго

**Решение:**
1. Уменьшите размер документа
2. Используйте более быструю модель
3. Проверьте нагрузку на LM Studio

### Граф не отображается

**Проблема**: Вкладка "Graph" пустая

**Решение:**
1. Убедитесь что документы проиндексированы
2. Подождите завершения индексации
3. Перезагрузите страницу

## Сравнение: WebUI vs Custom API

| Параметр | WebUI (9621) | Custom Service (8002) |
|----------|--------------|------------------------|
| **Интерфейс** | ✅ Веб UI | ❌ Только API |
| **Визуализация** | ✅ Граф знаний | ❌ Нет |
| **API** | ✅ REST API | ✅ REST API |
| **Swagger** | ✅ Да | ✅ Да |
| **Настройки** | ✅ Через UI | ⚙️ Через env |
| **Удобство** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Кастомизация** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**Рекомендация**: Используйте WebUI для экспериментов и визуализации, Custom Service для production интеграций.

## Производительность

### Ресурсы

- **RAM**: ~500-700 MB
- **CPU**: Низкое потребление
- **Диск**: ~200 MB (образ) + данные
- **Сеть**: Минимальное

### Скорость

- **UI загрузка**: < 1 сек
- **API ответ**: 50-200 ms
- **Индексация**: зависит от LM Studio
- **Поиск**: 50-150 ms (hybrid)

## Безопасность

⚠️ **Для разработки**:
- WebUI не имеет аутентификации
- API открыт для всех

🔒 **Для production**:
- Используйте reverse proxy (nginx/traefik)
- Добавьте Basic Auth или JWT
- Ограничьте доступ через firewall
- Используйте HTTPS

## Обновление

### Docker

```bash
# Пересборка образа с последней версией
docker-compose build --no-cache lightrag-webui
docker-compose up -d lightrag-webui
```

### Локальная установка

```bash
cd LightRAG
git pull
pip install -e ".[api]"
cd lightrag_webui
bun install
bun run build
```

## Логирование

```bash
# Просмотр логов
docker logs -f lection6_lightrag_webui

# Последние 100 строк
docker logs lection6_lightrag_webui --tail 100

# С временными метками
docker logs lection6_lightrag_webui --timestamps
```

## Порты

| Сервис | Порт | Описание |
|--------|------|----------|
| WebUI | 9621 | Веб-интерфейс + API |
| LightRAG Service | 8002 | Custom REST API |
| RAG Service | 8000 | Основной RAG |
| Docling | 8001 | Обработка документов |

## Ресурсы

- **Репозиторий**: https://github.com/HKUDS/LightRAG
- **Документация**: https://lightrag.github.io/
- **Issues**: https://github.com/HKUDS/LightRAG/issues

## Лицензия

MIT License (как и основной проект LightRAG)

---

**Создано для проекта MAI Lection 6** 🎓










