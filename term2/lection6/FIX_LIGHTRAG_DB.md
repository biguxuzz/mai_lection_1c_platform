# Исправление ошибки LightRAG WebUI: IndexError в nano_vectordb

## Проблема

При попытке индексировать документы в LightRAG WebUI возникает ошибка:

```
IndexError: index 0 is out of bounds for axis 0 with size 0
Failed to extract document
```

## Причина

Поврежденная или неправильно инициализированная база данных `nano_vectordb`. Это может произойти при:
- Первом запуске с пустой базой данных
- Неправильной инициализации векторной базы данных
- Проблемах с получением эмбеддингов из LMStudio

## Решение

### Вариант 1: Очистка базы данных (рекомендуется)

**Windows (PowerShell):**
```powershell
# Остановка контейнера
docker stop lection6_lightrag_webui

# Удаление volume с данными
docker volume rm lection6_lightrag_webui_data

# Перезапуск контейнера
docker-compose up -d lightrag-webui
```

**Linux/Mac (Bash):**
```bash
# Остановка контейнера
docker stop lection6_lightrag_webui

# Удаление volume с данными
docker volume rm lection6_lightrag_webui_data

# Перезапуск контейнера
docker-compose up -d lightrag-webui
```

### Вариант 2: Использование скрипта исправления

**Windows:**
```powershell
.\lightrag-webui-service\fix_db.ps1
```

**Linux/Mac:**
```bash
chmod +x lightrag-webui-service/fix_db.sh
./lightrag-webui-service/fix_db.sh
```

### Вариант 3: Пересборка контейнера

Если проблема сохраняется, пересоберите контейнер:

```bash
docker-compose down lightrag-webui
docker volume rm lection6_lightrag_webui_data
docker-compose build --no-cache lightrag-webui
docker-compose up -d lightrag-webui
```

## Проверка исправления

1. **Проверьте логи контейнера:**
```bash
docker logs lection6_lightrag_webui
```

Вы должны увидеть:
```
✅ Подключение успешно! Размерность эмбеддинга: 768
🚀 Запуск сервера...
```

2. **Проверьте доступность WebUI:**
```bash
curl http://localhost:9621/
```

3. **Попробуйте индексировать документ через WebUI:**
   - Откройте http://localhost:9621
   - Перейдите во вкладку "Documents"
   - Загрузите тестовый документ

## Профилактика

Скрипт `start.py` теперь автоматически:
- ✅ Проверяет подключение к LMStudio перед запуском
- ✅ Тестирует генерацию эмбеддингов
- ✅ Предупреждает о проблемах с базой данных

Если проблема повторяется, проверьте:
1. **LMStudio запущен** и доступен по адресу `http://host.docker.internal:1234`
2. **Модель эмбеддингов загружена** в LMStudio
3. **Правильные переменные окружения** в `docker-compose.yml`:
   - `EMBEDDING_BINDING_HOST=http://host.docker.internal:1234/v1`
   - `EMBEDDING_MODEL=text-embedding-qwen3-embedding-4b`
   - `EMBEDDING_DIM=768`

## Дополнительная информация

Подробная документация доступна в файле:
- `lightrag-webui-service/README.md`

Для получения помощи:
- Проверьте логи: `docker logs lection6_lightrag_webui`
- Проверьте статус контейнера: `docker ps | grep lightrag`
- Проверьте подключение к LMStudio: откройте `http://localhost:1234` в браузере

