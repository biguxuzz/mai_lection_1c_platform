# Семинар 8: Работа с веб-сервисами в 1С:Предприятие

**Продолжительность:** 90 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель семинара
Познакомить студентов с практической работой с веб-сервисами в платформе 1С:Предприятие, включая подключение к внешним сервисам, получение данных и сохранение их в базу данных.

## Необходимые предзнания
- Основы работы с платформой 1С:Предприятие
- Понятие конфигурации и метаданных
- Работа со справочниками и регистрами сведений
- Основы работы с запросами

---

## План семинара (90 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины практической работы и вопросов студентов.

### Блок 1: Введение в веб-сервисы (10 минут)

#### 1.1 Преимущества веб-сервисов

**Веб-сервисы** — это стандартизированный способ взаимодействия между приложениями через интернет.

**Преимущества:**
- Все методы и параметры уже описаны
- Можно изучить описание в WSDL (Web Services Description Language)
- API документация обычно доступна
- Прозрачная работа с данными

**Примеры использования:**
- Получение курсов валют
- Интеграция с банковскими системами
- Обмен данными с внешними системами
- Получение справочной информации

**Важно:** Веб-сервисы позволяют работать с внешними системами без необходимости знать их внутреннюю структуру.

---

#### 1.2 Пример веб-сервиса: получение курсов валют

**Задача:** Получить динамический курс валюты по датам через веб-сервис Центрального банка РФ.

**Метод веб-сервиса:**
- `GetCourseOnDate` — получение курса валюты на конкретную дату
- Параметры: код валюты, дата
- Возвращает: курс валюты

**Дополнительные возможности:**
- `GetCursDynamic` — получение курсов валют за диапазон дат
- Параметры: код валюты, дата начала, дата окончания
- Возвращает: коллекцию курсов за период

---

### Блок 2: Подключение веб-сервиса (20 минут)

#### 2.1 Создание ссылки на веб-сервис

**Шаги подключения:**

1. **Создание ссылки на веб-сервис:**
   - В конфигураторе создаем ссылку на веб-сервис
   - Указываем URI пространства имен (например, `http://www.cbr.ru/DailyInfoWebServ/DailyInfo.asmx`)
   - Указываем WSDL описание (например, `http://www.cbr.ru/DailyInfoWebServ/DailyInfo.asmx?WSDL`)

2. **Настройка свойств:**
   - Пространство имен: `http://web.cbr.ru/`
   - Описание: `DailyInfo`
   - Метод: `GetCourseOnDate`

**Важно:** Все необходимые параметры и типы данных описаны в WSDL или API документации.

---

#### 2.2 Настройка прокси и типов

**Прокси веб-сервиса:**
- Прокси — это локальное представление веб-сервиса в конфигурации
- Создается автоматически при подключении веб-сервиса
- Позволяет вызывать методы веб-сервиса из кода

**Типы объектов:**
- Веб-сервис использует типы данных для параметров и результатов
- Типы создаются автоматически на основе WSDL
- Пример: `GetCourseOnDate` принимает параметр типа `GetCursOnDate`

**Настройка параметров:**
```1c
// Получение прокси веб-сервиса
Прокси = WSСсылки.ПолучениеКурсаВалют.СоздатьWSПрокси();

// Получение типа параметра
ТипПараметра = Прокси.ТипыОбъектов.GetCursOnDate;

// Создание и заполнение параметра
Параметр = Прокси.СоздатьТип(ТипПараметра);
Параметр.OnDate = ТекущаяДата();
```

---

#### 2.3 Вызов метода веб-сервиса

**Структура вызова:**
1. Получить прокси веб-сервиса
2. Получить тип параметра
3. Создать и заполнить параметр
4. Вызвать метод веб-сервиса
5. Обработать результат

**Пример кода:**
```1c
// Получение прокси
Прокси = WSСсылки.ПолучениеКурсаВалют.СоздатьWSПрокси();

// Получение типа и создание параметра
ТипПараметра = Прокси.ТипыОбъектов.GetCursOnDate;
Параметр = Прокси.СоздатьТип(ТипПараметра);
Параметр.OnDate = ТекущаяДата();

// Вызов метода
Результат = Прокси.GetCourseOnDate(Параметр);
```

**Важно:** Пространство имен должно совпадать с пространством имен веб-сервиса.

---

### Блок 3: Обработка результата веб-сервиса (15 минут)

#### 3.1 Структура результата

**Результат вызова веб-сервиса:**
- Возвращается объект с типом ответа
- Содержит свойства с данными
- Может содержать коллекции объектов

**Пример структуры результата:**
```1c
Результат = Прокси.GetCourseOnDate(Параметр);

// Результат содержит:
// - Response (ответ)
//   - ValuteCursOnDate (коллекция курсов)
//     - Vname (наименование валюты)
//     - Vnom (номинал)
//     - Vcurs (курс)
//     - Vcode (код валюты)
//     - VchCode (числовой код валюты)
```

**Особенности:**
- Результат может быть коллекцией объектов
- Каждый объект содержит свойства с данными
- Нужно обойти коллекцию для получения всех значений

---

#### 3.2 Извлечение данных из результата

**Работа с коллекцией:**
```1c
// Получение коллекции курсов
КоллекцияКурсов = Результат.Response.ValuteCursOnDate;

// Обход коллекции
Для Каждого Курс Из КоллекцияКурсов Цикл
    
    // Извлечение данных
    КодВалюты = Курс.Vcode;
    Наименование = Курс.Vname;
    Номинал = Курс.Vnom;
    КурсВалюты = Курс.Vcurs;
    
КонецЦикла;
```

**Интересующие свойства:**
- `Vcode` — код валюты (например, USD, EUR)
- `Vcurs` — курс валюты
- `Vnom` — номинал (кратность валюты)
- `Vname` — наименование валюты

**Важно:** Структура результата зависит от конкретного веб-сервиса и описана в его документации.

---

### Блок 4: Сохранение данных в базу (25 минут)

#### 4.1 Создание справочника валют

**Необходимость справочника:**
- Для хранения информации о валютах
- Для связи курсов валют с валютами

**Структура справочника:**
- Код валюты (строка, длина 3) — например, USD, EUR, RUB
- Наименование (строка, длина 25)
- Автозаполнение по коду

**Настройка:**
- Справочник "Валюты"
- Размещение в подсистеме "Сервис"
- Код валюты используется для поиска

**Пример создания элемента:**
```1c
// Поиск валюты по коду
Валюта = Справочники.Валюты.НайтиПоКоду(КодВалюты);

// Если не найдена — создаем новую
Если Валюта.Пустая() Тогда
    Валюта = Справочники.Валюты.СоздатьЭлемент();
    Валюта.Код = КодВалюты;
    Валюта.Наименование = Наименование;
    Валюта.Записать();
КонецЕсли;
```

---

#### 4.2 Создание регистра сведений для курсов

**Назначение регистра:**
- Хранение курсов валют по датам
- Периодический регистр (периодичность: день)
- Независимый регистр (можно изменять напрямую)

**Структура регистра:**
- Измерение: Валюта (справочник Валюты)
- Периодичность: День
- Ресурсы:
  - Курс (Число 15,2)
  - Кратность (Число 15,2)

**Настройка:**
- Регистр сведений "КурсыВалют"
- Периодический, независимый
- Размещение в подсистеме "Сервис"

**Важно:** Периодический регистр позволяет хранить курс валюты на каждую дату отдельно.

---

#### 4.3 Запись данных в регистр

**Процесс записи:**
1. Получить курс из результата веб-сервиса
2. Найти или создать валюту в справочнике
3. Создать запись в регистре сведений
4. Заполнить данные
5. Записать запись

**Пример кода:**
```1c
// Обход коллекции курсов
Для Каждого Курс Из КоллекцияКурсов Цикл
    
    // Поиск валюты
    Валюта = Справочники.Валюты.НайтиПоКоду(Курс.Vcode);
    
    Если Не Валюта.Пустая() Тогда
        
        // Создание менеджера записи
        МенеджерЗаписи = РегистрыСведений.КурсыВалют.СоздатьМенеджерЗаписи();
        МенеджерЗаписи.Период = ТекущаяДатаСеанса();
        МенеджерЗаписи.Валюта = Валюта;
        МенеджерЗаписи.Курс = Курс.Vcurs;
        МенеджерЗаписи.Кратность = Курс.Vnom;
        
        // Запись с замещением существующей записи
        МенеджерЗаписи.Записать(РежимЗаписиРегистраСведений.Замещать);
        
    КонецЕсли;
    
КонецЦикла;
```

**Режим записи:**
- `Замещать` — если запись с такими измерениями и периодом уже существует, она будет заменена
- Полезно при повторном получении курсов на ту же дату

---

### Блок 5: Работа с диапазоном дат (10 минут)

#### 5.1 Метод GetCursDynamic

**Назначение:**
- Получение курсов валют за диапазон дат
- Более эффективно, чем множественные вызовы `GetCourseOnDate`

**Параметры:**
- `FromDate` — дата начала периода
- `ToDate` — дата окончания периода
- `ValutaCode` — код валюты

**Возвращаемое значение:**
- Коллекция курсов за указанный период
- Каждый элемент содержит курс на конкретную дату

**Пример использования:**
```1c
// Получение типа параметра
ТипПараметра = Прокси.ТипыОбъектов.GetCursDynamic;

// Создание и заполнение параметра
Параметр = Прокси.СоздатьТип(ТипПараметра);
Параметр.FromDate = НачалоПериода;
Параметр.ToDate = КонецПериода;
Параметр.ValutaCode = "USD";

// Вызов метода
Результат = Прокси.GetCursDynamic(Параметр);

// Обработка коллекции курсов
КоллекцияКурсов = Результат.GetCursDynamicResult;
```

---

#### 5.2 Обработка диапазона курсов

**Запись курсов за период:**
```1c
// Обход коллекции курсов
Для Каждого Курс Из КоллекцияКурсов Цикл
    
    // Создание записи для каждой даты
    МенеджерЗаписи = РегистрыСведений.КурсыВалют.СоздатьМенеджерЗаписи();
    МенеджерЗаписи.Период = Курс.OnDate; // Дата из результата
    МенеджерЗаписи.Валюта = Валюта;
    МенеджерЗаписи.Курс = Курс.Vcurs;
    МенеджерЗаписи.Кратность = Курс.Vnom;
    
    МенеджерЗаписи.Записать(РежимЗаписиРегистраСведений.Замещать);
    
КонецЦикла;
```

**Преимущества:**
- Один запрос вместо множества запросов
- Более быстрое заполнение регистра
- Меньше нагрузка на веб-сервис

---

### Блок 6: Аутентификация в веб-сервисах (5 минут)

#### 6.1 Типы аутентификации

**Общедоступные сервисы:**
- Не требуют аутентификации
- Пример: сервис курсов валют ЦБ РФ
- Можно вызывать без дополнительных настроек

**Защищенные сервисы:**
- Требуют аутентификации
- Используют логин и пароль
- Или токены доступа

**Настройка аутентификации:**
```1c
// Создание прокси с аутентификацией
Прокси = WSСсылки.ВебСервис.СоздатьWSПрокси();

// Установка учетных данных
Прокси.ИспользоватьАутентификацию = Истина;
Прокси.ИмяПользователя = "логин";
Прокси.Пароль = "пароль";
```

**Важно:** Способ аутентификации зависит от конкретного веб-сервиса и описан в его документации.

---

### Блок 7: Создание обработки для получения курсов (5 минут)

#### 7.1 Структура обработки

**Назначение обработки:**
- Удобный интерфейс для получения курсов валют
- Возможность выбора даты или диапазона дат
- Автоматическое сохранение в регистр

**Элементы формы:**
- Поле выбора даты (или диапазона дат)
- Кнопка "Получить курсы"
- Индикатор выполнения операции

**Размещение:**
- Обработка "ПолучениеКурсовВалют"
- Размещение в подсистеме "Сервис"
- Внешняя обработка (можно выгрузить отдельно)

---

## Резюме семинара (5 минут)

### Ключевые моменты:

1. **Веб-сервисы** — стандартизированный способ взаимодействия с внешними системами
   - Все методы и параметры описаны в WSDL или API
   - Прозрачная работа с данными
   - Не требуется знание внутренней структуры системы

2. **Подключение веб-сервиса:**
   - Создание ссылки на веб-сервис
   - Указание URI и WSDL
   - Настройка прокси и типов объектов

3. **Вызов метода веб-сервиса:**
   - Получение прокси
   - Создание и заполнение параметров
   - Вызов метода и обработка результата

4. **Обработка результата:**
   - Результат может быть коллекцией объектов
   - Каждый объект содержит свойства с данными
   - Необходимо обойти коллекцию для извлечения данных

5. **Сохранение данных:**
   - Создание справочника для хранения валют
   - Создание периодического регистра сведений для курсов
   - Запись данных с использованием менеджера записи

6. **Работа с диапазоном дат:**
   - Использование метода `GetCursDynamic` для получения курсов за период
   - Более эффективно, чем множественные вызовы
   - Один запрос вместо множества запросов

7. **Аутентификация:**
   - Общедоступные сервисы не требуют аутентификации
   - Защищенные сервисы требуют учетных данных
   - Настройка через свойства прокси

---

### Практические рекомендации:

1. **Изучение веб-сервиса:**
   - Изучите WSDL или API документацию перед началом работы
   - Определите необходимые методы и параметры
   - Понять структуру результата

2. **Обработка ошибок:**
   - Обрабатывайте исключения при вызове веб-сервиса
   - Проверяйте наличие данных в результате
   - Логируйте ошибки для отладки

3. **Оптимизация:**
   - Используйте методы для получения данных за период, если доступны
   - Кэшируйте данные в регистре сведений
   - Избегайте множественных вызовов для одних и тех же данных

4. **Структура данных:**
   - Используйте справочники для хранения справочной информации
   - Используйте периодические регистры для данных, изменяющихся во времени
   - Правильно настраивайте периодичность регистра

5. **Тестирование:**
   - Тестируйте вызовы веб-сервиса в консоли кода
   - Используйте точки останова для отладки
   - Проверяйте структуру результата перед обработкой

---

### Вопросы для самопроверки:

1. Какие преимущества имеют веб-сервисы при работе с внешними системами?
2. Что такое WSDL и для чего он используется?
3. Что такое прокси веб-сервиса и как его получить?
4. Как создать и заполнить параметр для вызова метода веб-сервиса?
5. Как обработать коллекцию объектов, возвращаемую веб-сервисом?
6. Почему для хранения курсов валют используется периодический регистр сведений?
7. В чем разница между режимами записи регистра сведений?
8. Какие преимущества имеет метод `GetCursDynamic` по сравнению с множественными вызовами `GetCourseOnDate`?
9. Когда требуется аутентификация при работе с веб-сервисами?
10. Как организовать обработку для удобной работы с веб-сервисом?

---

## Практическое задание:

1. Подключите веб-сервис получения курсов валют ЦБ РФ
2. Создайте справочник "Валюты" с кодом и наименованием
3. Создайте периодический регистр сведений "КурсыВалют" для хранения курсов
4. Реализуйте процедуру получения курсов на текущую дату
5. Реализуйте процедуру получения курсов за диапазон дат
6. Создайте обработку с формой для удобного получения курсов
7. Добавьте обработку ошибок при вызове веб-сервиса

---

**До встречи на следующем семинаре! Вопросы?** 🎓

