# Лекция 5: Базовые типы данных и коллекции в 1С:Предприятие

**Продолжительность:** 110 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель лекции
Познакомить студентов с примитивными типами данных, коллекциями значений и базовыми возможностями встроенного языка 1С:Предприятие для работы с данными.

## Необходимые предзнания
- Основы работы с платформой 1С:Предприятие
- Понятие конфигурации и метаданных
- Базовые концепции программирования

---

## План лекции (110 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины обсуждения практических примеров и вопросов студентов.

### Блок 1: Примитивные типы данных (20 минут)

#### 1.1 Булевый тип (3 минуты)

**Булевый тип** используется для хранения логических значений.

**Представление:**
- В коде: `Истина`, `Ложь` (или `True`, `False` на английском)
- Для пользователя: "Да", "Нет" (можно переопределить)

**Логические операции:**
- `И` (AND)
- `ИЛИ` (OR)
- `НЕ` (NOT)

**Пример:**
```1c
Переменная1 = Истина;
Переменная2 = Ложь;
Результат = Переменная1 И Переменная2; // Ложь
```

---

#### 1.2 Числовой тип (5 минут)

**Числовой тип** используется для хранения числовых значений.

**Особенности:**
- В коде разделитель дробной части — точка (`.`)
- Запятая (`,`) вызовет ошибку
- В представлении для пользователя зависит от локальных настроек системы
- Система автоматически добавляет разделители разрядов

**Арифметические операции:**
- Сложение (`+`)
- Вычитание (`-`)
- Умножение (`*`)
- Деление (`/`)
- Остаток от деления (`%`)

**Встроенные функции:**
- Математические функции доступны через синтаксис-помощник
- Глобальный контекст → Работа с числом

**Пример:**
```1c
Число1 = 10.5;
Число2 = 3;
Результат = Число1 + Число2; // 13.5
Остаток = Число1 % Число2; // 1.5
```

---

#### 1.3 Строковый тип (5 минут)

**Строковый тип** используется для хранения текстовых данных.

**Особенности:**
- В коде строка указывается в двойных кавычках (`"`)
- Автоматическое преобразование типов при операциях
- Число можно преобразовать в строку неявно
- Строку в число можно преобразовать, если она содержит корректное число

**Примеры:**
```1c
Строка1 = "Привет";
Строка2 = "123";
Число = 456;
Результат1 = Строка2 + Число; // "123456" (неявное преобразование)
Результат2 = Число(Строка2); // 123 (явное преобразование)
```

**Важно:** Если строка содержит запятую как разделитель дробной части, преобразование в число работает корректно.

---

#### 1.4 Тип Дата (5 минут)

**Тип Дата** используется для хранения даты и времени.

**Особенности:**
- В коде дата указывается в одинарных кавычках (`'`)
- Разделители не важны, система их убирает
- Можно указывать дату без времени, без секунд и т.д.

**Операции с датами:**
- Сложение даты с числом (преобразование в секунды)
- Для добавления дня: `Дата + 60 * 60 * 24` (секунды в дне)
- Дробная часть отсекается при преобразовании

**Пример:**
```1c
Дата1 = '2024.01.12';
Дата2 = Дата1 + 60; // Добавлено 60 секунд
Дата3 = Дата1 + 60 * 60 * 24; // Добавлен один день
```

---

#### 1.5 Составные типы (2 минуты)

**Составной тип** позволяет хранить в одном реквизите значения разных типов.

**Использование:**
- Для реквизитов, которые могут содержать разные типы данных
- Объект адресации может быть подразделением или ролью сотрудников
- Объект регистратор в регистрах сведений всегда составной

**Важно:** Лучше избегать составных типов, когда это возможно, для упрощения кода.

---

### Блок 2: Коллекции значений (40 минут)

#### 2.1 Массивы (10 минут)

**Массив** — базовая коллекция для хранения множества элементов.

**Особенности:**
- Нет типизации — можно хранить элементы разных типов
- Индексация начинается с нуля
- Доступ к элементам по индексу: `Массив[0]`
- Удаление по индексу: `Массив.Удалить(Индекс)`

**Методы массива:**
- `Добавить(Значение)` — добавление элемента
- `Удалить(Индекс)` — удаление по индексу
- `ВГраница()` — верхняя граница массива
- `Найти(Значение)` — поиск элемента

**Обход массива:**
```1c
Массив = Новый Массив;
Массив.Добавить("Элемент1");
Массив.Добавить(123);
Массив.Добавить(Дата());

Для Каждого Элемент Из Массив Цикл
    Сообщить(Элемент);
КонецЦикла;
```

**Доступ к элементам:**
- По индексу: `Массив[0]`
- Через квадратные скобки: `Массив[Индекс]`

---

#### 2.2 Структуры (15 минут)

**Структура** — коллекция с именованными элементами (ключ-значение).

**Особенности:**
- Доступ к элементам по ключу через точку: `Структура.Ключ`
- Доступ через квадратные скобки со строкой: `Структура["Ключ"]`
- Можно хранить вложенные структуры и массивы

**Создание структуры:**
```1c
Студент = Новый Структура;
Студент.Вставить("ФИО", "Иванов Иван Иванович");
Студент.Вставить("Группа", "ИТ-21");
Студент.Вставить("ДатаЗачисления", Дата());
```

**Обход структуры:**
```1c
Для Каждого Элемент Из Студент Цикл
    // Элемент содержит Ключ и Значение
    Сообщить(Элемент.Ключ + ": " + Элемент.Значение);
КонецЦикла;
```

**Важно:** Избегайте циклических ссылок в структурах!
- Структура1 содержит ссылку на Структуру2
- Структура2 содержит ссылку на Структуру1
- Это приводит к проблемам со сборщиком мусора и утечкам памяти

**Пример плохого кода:**
```1c
Структура1 = Новый Структура;
Структура2 = Новый Структура;
Структура1.Вставить("Структура2", Структура2);
Структура2.Вставить("Структура1", Структура1);
// Циклическая ссылка — сборщик мусора не сможет очистить память
```

---

#### 2.3 Список значений (10 минут)

**Список значений** — коллекция с пользовательским представлением.

**Особенности:**
- Имеет пользовательское представление (можно показать пользователю)
- Метод `ВыбратьЭлемент()` для выбора пользователем
- Каждый элемент имеет значение и представление

**Создание и использование:**
```1c
ВидТовара = Новый СписокЗначений;
ВидТовара.Добавить(1, "Услуга");
ВидТовара.Добавить(2, "Работа");
ВидТовара.Добавить(3, "Товар");

// Выбор элемента пользователем (синхронный)
ВыбранныйЭлемент = ВидТовара.ВыбратьЭлемент();

// Асинхронный выбор (для тонкого клиента)
Обещание = ВидТовара.ВыбратьЭлементАсинхронно();
Результат = Ожидать Обещание;
```

**Важно:** 
- Список значений существует только в памяти
- Не записывается в базу данных
- Используется для интерфейсных задач и вычислений

---

#### 2.4 Таблица значений (15 минут)

**Таблица значений** — двумерная структура данных, похожая на таблицу базы данных.

**Особенности:**
- Доступна только на сервере (не на клиенте)
- Состоит из колонок и строк
- Колонки определяют структуру данных
- Можно создавать индексы для ускорения поиска

**Создание таблицы значений:**
```1c
Студенты = Новый ТаблицаЗначений;
Студенты.Колонки.Добавить("Фамилия", Новый ОписаниеТипов("Строка"));
Студенты.Колонки.Добавить("Имя", Новый ОписаниеТипов("Строка"));
Студенты.Колонки.Добавить("Группа", Новый ОписаниеТипов("Строка"));
Студенты.Колонки.Добавить("ДатаЗачисления", Новый ОписаниеТипов("Дата"));
```

**Работа с данными:**
```1c
// Добавление строки
НоваяСтрока = Студенты.Добавить();
НоваяСтрока.Фамилия = "Иванов";
НоваяСтрока.Имя = "Иван";
НоваяСтрока.Группа = "ИТ-21";
НоваяСтрока.ДатаЗачисления = Дата();

// Поиск строк
СтруктураПоиска = Новый Структура;
СтруктураПоиска.Вставить("Фамилия", "Иванов");
СтруктураПоиска.Вставить("Имя", "Иван");
НайденныеСтроки = Студенты.НайтиСтроки(СтруктураПоиска);

Для Каждого НайденнаяСтрока Из НайденныеСтроки Цикл
    Сообщить(НайденнаяСтрока.Группа);
КонецЦикла;
```

**Индексы:**
- Можно создать индексы для ускорения поиска
- Индексы накладны при записи (перестраиваются)
- Полезны для больших таблиц значений

---

### Блок 3: Объектная модель работы с данными (20 минут)

#### 3.1 Архитектура 1С:Предприятие (10 минут)

**Два слоя системы:**

1. **База данных** — где хранятся данные
2. **Слой приложения** — где работает пользователь (сервер или клиент)

**Особенности:**
- Нет прямых SQL-операторов (INSERT, UPDATE, DELETE)
- Все операции через объектную модель
- Объекты создаются в памяти, запись происходит при вызове `Записать()`

**Менеджеры объектов:**
- `Справочники` — менеджер справочников
- `Документы` — менеджер документов
- Обращение: `Справочники.Контрагенты.НайтиПоНаименованию("ООО Рога и Копыта")`

**Жизненный цикл объекта:**
1. Создание: `НовыйЭлемент = Справочники.Номенклатура.СоздатьЭлемент()`
2. Заполнение реквизитов (в памяти)
3. Запись: `НовыйЭлемент.Записать()` → INSERT в базу

**Предопределенные элементы:**
- Создаются на уровне метаданных
- При применении изменений система создает их в базе
- Доступны через код: `Справочники.Контрагенты.РозничныйПокупатель`

---

#### 3.2 Работа с объектами (5 минут)

**Получение объекта:**
```1c
// Получение ссылки
Ссылка = Справочники.Контрагенты.НайтиПоНаименованию("ООО Рога и Копыта");

// Получение объекта по ссылке
Объект = Ссылка.ПолучитьОбъект();
Объект.Телефон = "+7 (495) 123-45-67";
Объект.Записать(); // UPDATE в базу
```

**Важно:** 
- Изменения происходят только в памяти до вызова `Записать()`
- `Записать()` выполняет INSERT или UPDATE в базу

---

#### 3.3 Язык запросов vs Объектная модель (5 минут)

**Язык запросов (SELECT):**
- Работа с табличным представлением
- Выбираются только нужные реквизиты
- Не загружает весь объект в память
- Предпочтительнее для больших объемов данных

**Объектная модель:**
- Работа с полным объектом
- При обращении через точку (`Объект.Реквизит`) происходит загрузка объекта в память
- Может быть медленнее на больших объемах

**Рекомендация:** Использовать запросы для выборки данных, объектную модель для изменения.

---

### Блок 4: Специальные типы (5 минут)

#### 4.1 Тип Неопределено

**Неопределено** — тип для переменных, которым еще не присвоено значение.

**Особенности:**
- Переменная объявлена, но значение не присвоено
- Неизвестно, какой тип будет у значения

**Пример:**
```1c
Переменная Неопределено;
// Переменная существует, но значение не определено
```

---

#### 4.2 Тип NULL

**NULL** — отсутствие объекта или значения.

**Получение NULL:**
- В запросах при LEFT JOIN, когда нет соответствующей записи
- При обращении к несуществующему элементу массива (обычно ошибка)

**Пример:**
```1c
// В запросе при LEFT JOIN
ВЫБРАТЬ
    Контрагенты.Ссылка,
    ДоговорыКонтрагентов.Ссылка КАК Договор
ИЗ
    Справочники.Контрагенты КАК Контрагенты
    ЛЕВОЕ СОЕДИНЕНИЕ Справочники.ДоговорыКонтрагентов КАК ДоговорыКонтрагентов
    ПО Контрагенты.Ссылка = ДоговорыКонтрагентов.Владелец
// Если договора нет, Договор будет NULL
```

---

### Блок 5: Встроенные функции (15 минут)

#### 5.1 Функции для работы со строками (10 минут)

**Доступ к функциям:**
- Синтаксис-помощник → Глобальный контекст → Работа со строками

**Основные функции:**

1. **СокрЛП()** — сокращение пробелов слева и справа
```1c
Строка = "   Привет   ";
Результат = СокрЛП(Строка); // "Привет"
```

2. **СтрРазделить()** — разделение строки на массив
```1c
Строка = "Элемент1 Элемент2 Элемент3";
МассивСтрок = СтрРазделить(Строка); // По пробелам
МассивСтрок = СтрРазделить(Строка, " "); // Явно указан разделитель
```

3. **СтрСоединить()** — соединение массива строк в одну строку
```1c
МассивСтрок = Новый Массив;
МассивСтрок.Добавить("Элемент1");
МассивСтрок.Добавить("Элемент2");
Результат = СтрСоединить(МассивСтрок, " "); // "Элемент1 Элемент2"
```

**Дополнительные возможности:**
- Работа с регулярными выражениями (для экспертов)
- Множество других функций для работы со строками

---

#### 5.2 Функции для работы с датами (5 минут)

**Особенности:**
- 1С:Предприятие — учетная система, много функций для работы с датами
- Функции для работы с периодами, неделями, днями

**Примеры функций:**
- `КонецДня()`, `НачалоДня()`
- `КонецНедели()`, `НачалоНедели()`
- `КонецМесяца()`, `НачалоМесяца()`
- И многие другие

**Доступ:** Синтаксис-помощник → Глобальный контекст → Работа с датой

---

### Блок 6: Асинхронное программирование (10 минут)

#### 6.1 Асинхронные вызовы

**Проблема:** Модальные вызовы блокируют работу приложения.

**Решение:** Асинхронные процедуры и методы.

**Пример асинхронной процедуры:**
```1c
&НаКлиенте
Процедура ВыбратьТовар()
    
    Обещание = ВидТовара.ВыбратьЭлементАсинхронно();
    Результат = Ожидать Обещание;
    
    Если Результат = Неопределено Тогда
        // Пользователь отказался
    Иначе
        // Обработка выбранного элемента
    КонецЕсли;
    
КонецПроцедуры
```

**Важно:**
- Асинхронные процедуры помечаются специальным образом
- Используется конструкция `Ожидать` для получения результата
- Приложение не блокируется во время выполнения

---

## Резюме лекции (5 минут)

### Ключевые моменты:

1. **Примитивные типы:**
   - Булевый (`Истина`, `Ложь`)
   - Число (разделитель — точка)
   - Строка (двойные кавычки)
   - Дата (одинарные кавычки)
   - Составные типы (для гибкости)

2. **Коллекции значений:**
   - Массив — индексированная коллекция без типизации
   - Структура — именованная коллекция (ключ-значение)
   - Список значений — коллекция с пользовательским представлением
   - Таблица значений — двумерная структура (только на сервере)

3. **Объектная модель:**
   - Работа через менеджеры объектов
   - Объекты создаются в памяти, запись при `Записать()`
   - Предопределенные элементы доступны через код
   - Запросы предпочтительнее для выборки данных

4. **Специальные типы:**
   - Неопределено — переменная без значения
   - NULL — отсутствие объекта

5. **Встроенные функции:**
   - Множество функций для работы со строками
   - Функции для работы с датами и периодами
   - Доступ через синтаксис-помощник

6. **Асинхронное программирование:**
   - Асинхронные процедуры для неблокирующих операций
   - Использование `Ожидать` для получения результатов

---

### Практические рекомендации:

1. **Работа с коллекциями:**
   - Избегайте циклических ссылок в структурах
   - Используйте типизацию колонок в таблицах значений
   - Создавайте индексы для больших таблиц значений

2. **Производительность:**
   - Используйте запросы для выборки данных
   - Избегайте загрузки полных объектов, если нужны только реквизиты
   - Таблицы значений доступны только на сервере

3. **Работа с типами:**
   - Используйте явное преобразование типов, когда возможно
   - Проверяйте на `Неопределено` и `NULL` перед использованием
   - Избегайте составных типов без необходимости

4. **Асинхронность:**
   - Используйте асинхронные методы для пользовательского интерфейса
   - Не блокируйте приложение модальными вызовами

---

### Вопросы для самопроверки:

1. В чем разница между массивом и структурой?
2. Почему таблица значений доступна только на сервере?
3. Что произойдет, если создать циклическую ссылку между структурами?
4. Когда использовать список значений вместо массива?
5. В чем разница между типом Неопределено и NULL?
6. Почему запросы предпочтительнее объектной модели для выборки данных?
7. Как добавить день к дате в 1С?
8. Какие функции используются для работы со строками?
9. Что такое асинхронная процедура и зачем она нужна?
10. Как получить значение из структуры по ключу?

---

## Следующая лекция:

**Лекция 6: [Тема следующей лекции]**

**До встречи на следующей лекции! Вопросы?** 🎓

