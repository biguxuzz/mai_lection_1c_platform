# Лекция 3: Роли и полномочия в 1С:Предприятие

**Продолжительность:** 87 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель лекции
Познакомить студентов с системой ролей и полномочий в 1С:Предприятие, научить настраивать права доступа пользователей, использовать отладку и анализ производительности при разработке.

## Необходимые предзнания
- Основы работы с платформой 1С:Предприятие
- Понятие конфигурации и метаданных
- Базовые объекты хранения данных (справочники, документы, регистры)

---

## План лекции (87 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины обсуждения практических примеров и вопросов студентов.

### Блок 1: Повторение предыдущего материала (10 минут)

#### 1.1 Пример: автоматизация библиотеки (10 минут)

**Задача:** Нужно автоматизировать библиотеку, где нужно вести записи книг, авторов, выдачи и приемки книг.

**Выбор объектов метаданных:**

1. **Книги** — справочник
   - Хранит информацию о каждой книге

2. **Авторы** — справочник
   - Как связать книгу и автора?
   - Если основной автор один — ссылка на справочник
   - Если соавторов несколько — табличная часть справочника "Книги" с реквизитом "Авторы"

3. **Выдача книг, приемка обратно** — документы
   - Фиксируют факты выдачи и возврата книг

4. **Где хранится информация об остатках книг?**
   - Документ не хранит информацию об остатках (только факт движения)
   - Можно использовать регистр сведений (книга + местоположение)
   - Или регистр накопления остатков (книга + читатель → количество выданных книг)
   - Для учета по экземплярам: каждый экземпляр книги — отдельный элемент справочника

5. **Где пользователь смотрит информацию?**
   - Отчеты — для просмотра данных из регистров

**Предопределенные документы:**
- Предопределенных документов нет (в отличие от справочников)
- Документ — отражение факта деятельности
- Для ввода начальных остатков используются обычные документы, но не предопределенные

---

### Блок 2: Выгрузка и загрузка конфигурации (5 минут)

#### 2.1 Выгрузка информационной базы (2 минуты)

**DT файл** содержит:
- Конфигурацию
- Данные (бизнес-данные)
- Пользователей (если они были добавлены)

**Что НЕ попадает в DT файл:**
- Журнал регистрации (привязан к конкретному экземпляру базы данных)
- Журнал регистрации логирует события системы и настраивается по периодам (по дням)

**Процесс выгрузки:**
- Администрирование → Выгрузить информационную базу

#### 2.2 Загрузка информационной базы (3 минуты)

**Процесс загрузки:**
- Администрирование → Загрузить информационную базу

**Примечание:** Для сохранения только конфигурации используется "Сохранить конфигурацию в файлы" — это сохраняет только конфигурацию, без данных.

---

### Блок 3: Роли и пользователи (20 минут)

#### 3.1 Создание ролей (5 минут)

**Роль** — объект метаданных в разделе "Общие" → "Роли".

**Первая роль — "Полные права":**
- Обязательно должна быть в системе
- Первый пользователь должен иметь полные права
- Иначе базу никто не сможет настраивать

**Настройка роли:**
- Правой кнопкой → "Установить все права"
- Важно: установить галочку "Установить права для новых объектов"
- Это позволяет автоматически получать права на новые объекты метаданных без ручной настройки

#### 3.2 Аутентификация пользователей (3 минуты)

**Типы аутентификации:**

1. **Аутентификация 1С:Предприятие**
   - Стандартная аутентификация через пароль
   - Настраивается в свойствах пользователя

2. **Аутентификация операционной системы**
   - Windows: подключение к домену
   - Linux: использование отдельных файлов (keytab)
   - Позволяет организовать беспарольный доступ

#### 3.3 Динамическое обновление конфигурации (5 минут)

**Механизм слоев:**
- При изменении конфигурации пользователи продолжают работать в предыдущем слое
- Динамическое обновление добавляет новый слой
- Слои склеиваются при реструктуризации базы данных

**Важно:** На продуктивных средах динамическое обновление не рекомендуется, так как каждый слой снижает производительность.

#### 3.4 Создание пользователей с ограниченными правами (7 минут)

**Пример:** Создание ролей "Закупки" и "Продажи"

**Права запуска:**
- По умолчанию создаются права запуска
- Пользователь с только правами запуска может войти в систему, но ничего не видит

**Настройка прав доступа:**
- Права настраиваются для каждого объекта метаданных отдельно
- Можно настроить на уровне всего справочника или конкретного справочника

---

### Блок 4: Интерактивные и неинтерактивные права (15 минут)

#### 4.1 Неинтерактивные права (5 минут)

**Неинтерактивные права** используются при работе через код:
- **Чтение** — выполнение запросов к данным
- **Добавление** — создание объектов через код
- **Изменение** — изменение объектов через код
- **Удаление** — удаление объектов через код

**Пример:** Запрос `ВЫБРАТЬ ИЗ Справочники.Товары` требует права "Чтение".

#### 4.2 Интерактивные права (10 минут)

**Интерактивные права** используются в пользовательском режиме:
- **Просмотр** — открытие и просмотр объектов
- **Интерактивное добавление** — создание через форму
- **Интерактивное изменение** — редактирование через форму
- **Интерактивное удаление** — удаление через форму (не рекомендуется)
- **Интерактивная пометка на удаление** — пометка объектов на удаление
- **Интерактивное снятие пометки на удаление**
- **По строке** — возможность ввода по строке в реквизитах

**Важно:** 
- При добавлении интерактивного права автоматически добавляется соответствующее неинтерактивное право
- Интерактивное удаление в бизнес-системах практически не используется (опасно из-за возможности создания пустых ссылок)
- Вместо удаления используется пометка на удаление

**Предопределенные элементы:**
- При создании экземпляра базы автоматически создаются предопределенные элементы
- Можно изменить интерактивную ссылку предопределенного элемента
- Полезно при слиянии баз данных

---

### Блок 5: Права на уровне записи (RLS) (10 минут)

#### 5.1 Назначение RLS (2 минуты)

**RLS (Record Level Security)** — права на уровне записи.

**Зачем нужны:**
- Чтобы пользователи видели только определенные элементы
- Чтобы ограничить доступ к конкретным записям в рамках одного объекта

#### 5.2 Настройка RLS (8 минут)

**Пример:** Пользователь "Закупщик" не должен видеть объекты, помеченные на удаление.

**Процесс настройки:**
1. В роли выбрать объект (например, справочник "Товары")
2. Перейти на вкладку "Ограничения доступа к данным"
3. Добавить ограничение на поле "Ссылка" (основное поле справочника)
4. Указать условие: `НЕ Товары.ПометкаУдаления`
5. Применить на чтение (не на просмотр, так как просмотр — интерактивное право)

**Конструктор запросов:**
- Помогает составить шаблон ограничения
- Есть производительные шаблоны через регистр сведений (одна таблица вместо множества RLS)

**Результат:**
- Пользователь с ограничением не видит помеченные на удаление объекты
- Администратор без ограничений видит все объекты

---

### Блок 6: Права на документы (5 минут)

#### 6.1 Специфические права документов (5 минут)

**Дополнительные права для документов:**
- **Интерактивное проведение** — проведение документа в пользовательском режиме
- **Интерактивное отмена проведения** — отмена проведения
- **Интерактивное изменение проведенных** — изменение уже проведенных документов
- **Оперативное проведение** — проведение на текущий момент
- **Неоперативное проведение** — проведение документов вне текущего момента

**Важно:** При добавлении интерактивного права автоматически добавляется необходимое неинтерактивное право.

---

### Блок 7: Оперативное и неоперативное проведение (10 минут)

#### 7.1 Оперативное проведение (5 минут)

**Оперативное проведение** — проведение документа на текущий момент времени.

**Особенности:**
- Можно провести только документ с текущей датой
- Нельзя провести документ с прошлой или будущей датой
- Граница определяется последним проведенным документом

**Пример использования:**
- Массовый ввод документов (кассовые чеки)
- Множество пользователей работают одновременно
- Быстрое действие без пересчета итогов

#### 7.2 Неоперативное проведение (5 минут)

**Неоперативное проведение** — проведение документов вне текущего момента.

**Проблема:**
- При редактировании уже проведенного документа может нарушиться последовательность проведения
- Другие пользователи могут провести документы, изменив данные
- Например, себестоимость товара может измениться

**Пример сценария:**
1. Документ проведен, товар получил себестоимость (например, FIFO)
2. Документ распроведен для редактирования
3. Другой пользователь провел следующий документ
4. Себестоимость уже изменилась
5. При повторном проведении логика может измениться

**Решение:**
- Для массового ввода использовать оперативное проведение
- Для документов, требующих редактирования, использовать неоперативное проведение

---

### Блок 8: Отладка (20 минут)

#### 8.1 Настройка отладки (5 минут)

**Проблема:** Пользователь без прав администратора не может отлаживать код.

**Решение:** Запуск отладки из-под другого пользователя.

**Процесс:**
1. Сервис → Параметры → Запуск 1С:Предприятие
2. Указать пользователя для отладки
3. Запустить отладку под этим пользователем
4. В конфигураторе можно отлаживать под администратором

**Подключение к отладке:**
- Отладка → Подключение
- Видно подключения к серверу и тонкому клиенту
- Для отладки серверного кода нужно подключение к серверу

#### 8.2 Использование точек останова (5 минут)

**Точка останова** — остановка выполнения кода в определенной строке.

**Возможности:**
- Поставить точку останова в коде
- При выполнении код остановится на этой строке
- Можно просмотреть значения переменных
- Можно выполнить код по шагам

**Инспектор переменных:**
- Показывает все переменные в текущем контексте
- Можно вычислить выражение
- Можно посмотреть значения объектов

#### 8.3 Отладка по ошибке (5 минут)

**Подключение по ошибке:**
- Настройка: подключиться в момент возникновения ошибки
- Остановка по ошибке: код остановится на строке с ошибкой
- Можно посмотреть контекст выполнения

**Условия остановки:**
- Можно настроить условия для разных типов ошибок
- Некоторые ошибки — восстановимые (попытка, критические)
- Не всегда нужно останавливаться на всех ошибках

#### 8.4 Пример отладки (5 минут)

**Пример кода с задержкой:**
```1c
ДатаОкончания = ТекущаяДатаСеанса() + 10;
Пока ДатаОкончания > ТекущаяДатаСеанса() Цикл
    Счетчик = Счетчик + 1;
КонецЦикла;
```

**Работа с объектами:**
- Разыминование ссылки в объект: `ЛокальныйДоговор = ДоговорПоставщика.ПолучитьОбъект()`
- Разыминование — не бесплатная операция (подтягивает все реквизиты объекта)
- Иногда выгоднее получить нужный реквизит запросом

**Пример изменения объекта:**
```1c
ЛокальныйДоговор = ДоговорПоставщика.ПолучитьОбъект();
ЛокальныйДоговор.ДатаНачала = НачалоГода(ТекущаяДатаСеанса());
ЛокальныйДоговор.ДатаОкончания = КонецГода(ТекущаяДатаСеанса());
ЛокальныйДоговор.Записать();
```

---

### Блок 9: Замер производительности (5 минут)

#### 9.1 Назначение замера производительности (2 минуты)

**Замер производительности** — инструмент для анализа времени выполнения кода.

**Зачем нужен:**
- Определить, какие строки кода выполняются дольше всего
- Найти узкие места в производительности
- Оптимизировать код

#### 9.2 Использование замера (3 минуты)

**Процесс:**
1. Включить замер производительности (таймер)
2. Выполнить код
3. Закрыть замер
4. Просмотреть результаты

**Результаты:**
- Показывает, какие строки выполнялись
- Показывает время выполнения каждой строки
- Помогает найти проблемные места

**Пример:** Запись объекта договора заняла больше всего времени — это нормально, так как это операция записи в базу данных.

**Примечание:** В режиме замера времени можно отследить, какой код выполняется и сколько времени занимает.

---

### Блок 10: Работа с сообщениями (2 минуты)

#### 10.1 Сообщения пользователю

**Важно:** Вместо процедуры `Сообщить()` использовать `СообщитьПользователю()`.

**Причина:**
- `Сообщить()` может работать в разном контексте
- Для показа сообщений пользователю нужно использовать специальную процедуру
- Сообщения с сервера не видны напрямую, нужно использовать правильный механизм

**Пример:**
```1c
СообщитьПользователю(Счетчик + " " + Строка(Счетчик));
```

**Примечание:** Неявное преобразование типов может вызвать ошибку, нужно явно преобразовывать типы.

---

## Резюме лекции (5 минут)

### Ключевые моменты:

1. **Выгрузка и загрузка конфигурации**
   - DT файл содержит конфигурацию и данные
   - Журнал регистрации не попадает в DT файл
   - Первый пользователь должен иметь полные права

2. **Роли и права доступа**
   - Роль — объект метаданных для настройки прав
   - Неинтерактивные права — для работы через код
   - Интерактивные права — для работы в пользовательском режиме
   - Интерактивное удаление в бизнес-системах не используется

3. **Права на уровне записи (RLS)**
   - Ограничение доступа к конкретным записям
   - Настраивается через конструктор запросов
   - Можно использовать производительные шаблоны

4. **Оперативное и неоперативное проведение**
   - Оперативное — проведение на текущий момент
   - Неоперативное — проведение вне текущего момента
   - Для массового ввода используется оперативное проведение

5. **Отладка**
   - Запуск отладки из-под другого пользователя
   - Точки останова для остановки выполнения
   - Отладка по ошибке для анализа проблем
   - Инспектор переменных для просмотра значений

6. **Замер производительности**
   - Инструмент для анализа времени выполнения
   - Помогает найти узкие места
   - Показывает время выполнения каждой строки

---

### Практические рекомендации:

1. **Настройка прав:**
   - Первый пользователь — обязательно с полными правами
   - Использовать пометку на удаление вместо интерактивного удаления
   - Настраивать права для новых объектов автоматически

2. **Производительность:**
   - Использовать оперативное проведение для массового ввода
   - Избегать динамического обновления на продуктивных средах
   - Использовать замер производительности для оптимизации

3. **Отладка:**
   - Настраивать отладку сервера для серверного кода
   - Использовать точки останова для анализа выполнения
   - Подключаться по ошибке для анализа проблем

4. **RLS:**
   - Использовать производительные шаблоны для сложных ограничений
   - Ограничения настраивать на чтение, а не на просмотр

---

### Вопросы для самопроверки:

1. Что содержит DT файл при выгрузке информационной базы?
2. Почему первый пользователь должен иметь полные права?
3. В чем разница между интерактивными и неинтерактивными правами?
4. Почему интерактивное удаление не используется в бизнес-системах?
5. Что такое RLS и для чего он используется?
6. В чем разница между оперативным и неоперативным проведением?
7. Как запустить отладку из-под другого пользователя?
8. Для чего используется замер производительности?
9. Как настроить ограничение доступа к помеченным на удаление объектам?
10. Почему важно использовать `СообщитьПользователю()` вместо `Сообщить()`?

---

## Следующая лекция:

**Лекция 4: Работа с формами и параметрами сеанса в 1С:Предприятие**

**План лекции:**
1. Проектные технологии разработки
   - Технология Do and Fix
   - Технология PMBOK (Водопад)
   - Agile-манифест и методологии (Scrum, Kanban)
2. Инструмент разработки EDT
   - Общие сведения об EDT
   - 1С:Напарник
   - Ограничения EDT
3. Блокировки данных
   - Объектные блокировки (оптимистичные, пессимистичные)
   - Транзакционные блокировки (разделяемые, эксклюзивные)
4. Доработка документов поступления и реализации
   - Исправление документа поступления товаров
   - Создание документа реализации товаров
5. Формы выбора с остатками
   - Проблема: необходимость видеть остатки
   - Создание формы выбора с остатками
   - Настройка динамического списка
6. Параметризация формы выбора по складу
   - Передача параметра склада
   - Использование параметра в форме выбора
7. Параметры сеанса
   - Назначение параметров сеанса
   - Создание справочника пользователей
   - Инициализация параметров сеанса

**До встречи на следующей лекции! Вопросы?** 🎓

