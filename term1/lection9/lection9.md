# Лекция 9: Работа с хранилищем в 1С:Предприятие

**Продолжительность:** 60 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель лекции
Познакомить студентов с механизмами работы с хранилищем в платформе 1С:Предприятие, включая временное хранилище для передачи данных между формами и хранилище значений для работы с изображениями.

## Необходимые предзнания
- Основы работы с формами в 1С:Предприятие
- Понимание клиент-серверной архитектуры платформы
- Работа с табличными частями документов
- Базовые концепции работы с данными

---

## План лекции (60 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины обсуждения практических примеров и вопросов студентов.

### Блок 1: Введение в хранилище (5 минут)

#### 1.1 Назначение хранилища (5 минут)

**Хранилище** в платформе 1С используется для двух основных задач:

1. **Передача данных между формами**
   - Формы работают только на клиенте
   - Сложные объекты нельзя передать напрямую между формами
   - Необходим механизм промежуточного хранения

2. **Работа с картинками на форме**
   - Хранение изображений в объектах конфигурации
   - Отображение картинок в элементах формы

**Пример использования:** Документ реализации товаров, где нужно подобрать несколько товаров через отдельную форму подбора.

---

### Блок 2: Временное хранилище для передачи данных (30 минут)

#### 2.1 Создание формы подбора (10 минут)

**Задача:** Реализовать групповой подбор товаров в документе реализации.

**Шаги реализации:**

1. **Создание кнопки подбора**
   - Размещение кнопки над табличной частью "Товары"
   - Кнопка будет клиентской процедурой

2. **Создание формы подбора**
   - Копирование формы выбора с остатками
   - Создание новой формы "ФормаПодбораСОстатками"
   - Передача параметров (склад) в форму подбора

**Пример кода передачи параметров:**
```1c
// Создание структуры параметров
ПараметрыПодбора = Новый Структура;
ПараметрыПодбора.Вставить("Склад", Объект.Склад);

// Открытие формы с параметрами и владельцем
ОткрытьФорму("Справочник.Товары.ФормаПодбораСОстатками", ПараметрыПодбора, ЭтаФорма);
```

---

#### 2.2 Организация списка выбора (10 минут)

**Создание таблицы значений для подобранных товаров:**

**Структура таблицы:**
- Товар (СправочникСсылка.Товары)
- Количество (Число, 15,3)
- Цена (Суммовой)
- Сумма (Суммовой)

**Особенности заполнения:**

1. **Товар** — это элемент выбора из динамического списка
   - Основной реквизит формы списка — товары
   - Элемент выбора будет ссылкой на товар

2. **Количество** — по умолчанию равно единице

3. **Цена** — берется из выбранной строки
   - Используется `ТекущиеДанные` выбранной строки
   - Необходимо добавить цену в настройки списка формы

4. **Сумма** — рассчитывается как цена × количество

**Переопределение выбора:**
```1c
// В форме списка переопределяем обработку выбора
СтандартнаяОбработка = Ложь; // Не закрываем форму при выборе

// Добавление строки в таблицу подобранных товаров
НоваяСтрока = ПодобранныеТовары.Добавить();
НоваяСтрока.Товар = Элемент;
НоваяСтрока.Количество = 1;
НоваяСтрока.Цена = ТекущиеДанные.Цена;
НоваяСтрока.Сумма = НоваяСтрока.Цена * НоваяСтрока.Количество;
```

---

#### 2.3 Передача данных через временное хранилище (10 минут)

**Механизм работы:**

1. **Помещение данных во временное хранилище**
   - Создание серверной функции, возвращающей адрес
   - Использование метода `ПоместитьВоВременноеХранилище()`
   - Выгрузка таблицы значений с помощью `Выгрузить()`

**Пример кода:**
```1c
// Серверная функция
Функция СовершитьПодборНаСервере()
    Адрес = ПоместитьВоВременноеХранилище(ПодобранныеТовары.Выгрузить(), УникальныйИдентификаторФормы);
    Возврат Адрес;
КонецФункции
```

**Особенности временного хранилища:**

- Создает файл в папке `cntx` на сервере сессии
- Без указания уникального модификатора хранится до конца сессии
- **Важно:** Указывать уникальный идентификатор формы для автоматической очистки
- Данные нужны только на время жизни формы подбора

2. **Получение данных из временного хранилища**

**На форме-владельце (документ реализации):**

```1c
// Экспортная процедура на клиенте
Процедура ОбработатьПодбор(Адрес) Экспорт
    ОбработатьПодборНаСервере(Адрес);
    Закрыть(); // Закрываем форму подбора
КонецПроцедуры

// Серверная процедура
Процедура ОбработатьПодборНаСервере(Адрес)
    ДанныеПодбора = ПолучитьИзВременногоХранилища(Адрес);
    Объект.Товары.Загрузить(ДанныеПодбора);
КонецПроцедуры
```

**Почему нельзя передать табличную часть напрямую:**

- Табличная часть — это объект, которого нет в клиентском контексте
- В клиентском контексте есть только элементы формы, отображающие табличную часть
- Попытка передачи приведет к ошибке: "Попытка передать мутабельное значение"

---

### Блок 3: Хранилище значений для картинок (25 минут)

#### 3.1 Добавление реквизита для картинки (5 минут)

**Проблема:** Система не понимает, как работать с хранилищем значений на формах.

**Решение:**

1. **В объекте конфигурации:**
   - Добавить реквизит "КартинкаДанные" типа `ХранилищеЗначений`

2. **На форме:**
   - Добавить реквизит формы "АдресКартинки" типа `Строка`
   - Добавить элемент формы "ПолеКартинки" (не поле ввода!)
   - Привязать элемент к реквизиту "АдресКартинки"

**Важно:** Хранилище значений нельзя напрямую отобразить на форме, поэтому используется промежуточный реквизит-строка с адресом.

---

#### 3.2 Загрузка картинки (10 минут)

**Создание команды загрузки:**

**Асинхронный вызов для выбора файла:**

```1c
// Клиентская процедура с асинхронным вызовом
&НаКлиенте
Процедура ЗагрузитьКартинку()
    Обещание = ПоместитьФайлНаСерверАсинхронно(УникальныйИдентификаторФормы);
    
    Если Обещание = Неопределено Тогда
        Возврат; // Пользователь отменил выбор
    КонецЕсли;
    
    АдресКартинки = Обещание.АдресФайла;
    Модифицированность = Истина; // Устанавливаем флаг модификации
КонецПроцедуры
```

**Особенности асинхронных вызовов:**

- Синтаксис похож на Python (`async`/`await`)
- Работа с обещаниями (promises)
- Пользователь может отменить выбор (вернется `Неопределено`)
- Форма остается немодальной — можно продолжать работу

**Альтернативный способ:** Организация через оповещения (более старый синтаксис, больше кода).

---

#### 3.3 Сохранение картинки в объект (10 минут)

**Обработка перед записью:**

```1c
// Серверная процедура
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
    // Проверяем, что адрес картинки — это адрес временного хранилища
    Если АдресВременногоХранилища(АдресКартинки) Тогда
        // Получаем данные из временного хранилища
        ДанныеКартинки = ПолучитьИзВременногоХранилища(АдресКартинки);
        // Упаковываем в хранилище значений
        ТекущийОбъект.КартинкаДанные = Новый ХранилищеЗначений(ДанныеКартинки);
    КонецЕсли;
КонецПроцедуры
```

**Инициализация адреса при открытии:**

```1c
// Серверная процедура
Процедура ПриСозданииНаСервере(Отказ, ТекущийОбъект)
    // Получаем навигационную ссылку на картинку из объекта
    АдресКартинки = ПолучитьНавигационнуюСсылку(ТекущийОбъект, "КартинкаДанные");
КонецПроцедуры
```

**Важно:** 
- Флаг модифицированности устанавливается вручную при загрузке картинки
- Используется `ПередЗаписьюНаСервере`, а не `ПриЗаписиНаСервере` (объект уже записан)
- Навигационная ссылка позволяет отобразить картинку при открытии формы

---

## Резюме лекции (5 минут)

### Ключевые моменты:

1. **Временное хранилище** — механизм передачи данных между формами
   - Создает файлы в папке `cntx` на сервере
   - Хранится до конца сессии или до удаления по идентификатору
   - Используется для передачи таблиц значений и других сложных объектов

2. **Хранилище значений** — для работы с бинарными данными (картинки)
   - Нельзя напрямую отобразить на форме
   - Используется промежуточный реквизит-строка с адресом
   - Сохранение происходит через временное хранилище

3. **Асинхронные вызовы** — для работы с файлами
   - Современный синтаксис с обещаниями
   - Альтернатива — через оповещения (старый способ)

4. **Клиент-серверная архитектура**
   - Табличные части не существуют в клиентском контексте
   - Передача сложных объектов требует промежуточного хранения
   - Ошибка при прямой передаче: "Попытка передать мутабельное значение"

---

### Практические рекомендации:

1. **Передача данных между формами:**
   - Используйте временное хранилище для таблиц значений
   - Указывайте уникальный идентификатор формы для автоматической очистки
   - Передавайте адрес, а не сами данные

2. **Работа с картинками:**
   - Создавайте реквизит типа `ХранилищеЗначений` в объекте
   - Используйте промежуточный реквизит-строку на форме
   - Сохраняйте через `ПередЗаписьюНаСервере`
   - Инициализируйте адрес через `ПриСозданииНаСервере`

3. **Асинхронные вызовы:**
   - Используйте современный синтаксис с обещаниями
   - Проверяйте на `Неопределено` (пользователь отменил)
   - Устанавливайте флаг модифицированности вручную

4. **Производительность:**
   - Очищайте временное хранилище после использования
   - Используйте уникальные идентификаторы для автоматической очистки

---

### Вопросы для самопроверки:

1. Зачем нужно временное хранилище для передачи данных между формами?
2. Почему нельзя передать табличную часть напрямую из клиентского контекста?
3. Что происходит с данными во временном хранилище, если не указать уникальный идентификатор?
4. Почему хранилище значений нельзя напрямую отобразить на форме?
5. В каком событии нужно сохранять картинку в объект: `ПередЗаписьюНаСервере` или `ПриЗаписьюНаСервере`?
6. Что такое навигационная ссылка и зачем она нужна?
7. Как обработать ситуацию, когда пользователь отменил выбор файла в асинхронном вызове?
8. Почему нужно вручную устанавливать флаг модифицированности при загрузке картинки?
9. В чем разница между асинхронными вызовами через обещания и через оповещения?
10. Где физически хранятся данные временного хранилища?

---

## Следующая лекция:

**Лекция 10: Язык запросов в 1С:Предприятие**

**План лекции:**
1. Введение в язык запросов
   - Консоль запросов
   - Модели работы с данными
   - Ограничения языка запросов
2. Основы синтаксиса языка запросов
   - Синтаксис и сходство с SQL
   - Обращение к данным через точку
   - Обработка запросов сервером
   - Основные секции запроса
3. Конструктор запросов
   - Назначение и возможности
   - Работа с конструктором
4. Соединения таблиц
   - Типы соединений
   - Обработка NULL значений
5. Виртуальные таблицы
   - Назначение виртуальных таблиц
   - Виртуальные таблицы регистров накопления
   - Виртуальные таблицы регистров сведений
   - Оптимизация работы с виртуальными таблицами
6. Временные таблицы
   - Назначение временных таблиц
   - Работа с временными таблицами
   - Преимущества временных таблиц
7. Подзапросы
   - Назначение и синтаксис
   - Недостатки подзапросов
   - Рекомендации по использованию
8. Особенности языка запросов 1С
   - Ключевое слово ССЫЛКА
   - Представление (ПРЕДСТАВЛЕНИЕ)
   - Выразить (ВЫРАЗИТЬ)
   - Выбор (ВЫБОР) — аналог CASE
9. Оптимизация запросов и рекомендации
   - Рекомендации по оптимизации
   - Типичные ошибки

**До встречи на следующей лекции! Вопросы?** 🎓

