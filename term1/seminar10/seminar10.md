# Семинар 10: Работа с запросами в 1С:Предприятие

**Продолжительность:** 90 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель семинара
Научить студентов практической работе с запросами в 1С:Предприятие, включая использование конструктора запросов, работу с соединениями таблиц, транспонирование данных и правильное использование виртуальных таблиц регистров.

## Необходимые предзнания
- Основы работы с платформой 1С:Предприятие
- Понятие справочников и табличных частей
- Базовые знания языка запросов 1С
- Работа с регистрами накопления

---

## План семинара (90 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины практической работы и вопросов студентов.

### Блок 1: Введение в работу с запросами (5 минут)

#### 1.1 Консоль запросов

**Консоль запросов** — инструмент для написания и тестирования запросов в 1С:Предприятие.

**Назначение:**
- Написание и отладка запросов
- Тестирование запросов перед использованием в отчетах
- Изучение структуры данных

**Открытие консоли:**
- В режиме предприятия: через меню или внешнюю обработку
- В конфигураторе: через контекстное меню в макете запроса

**Важно:** Умение составлять запросы — основа для создания отчетов и аналитики.

---

### Блок 2: Конструктор запросов (10 минут)

#### 2.1 Назначение конструктора запросов

**Конструктор запросов** — визуальный инструмент для создания запросов без написания текста вручную.

**Преимущества:**
- Визуальное представление структуры запроса
- Автоматическая генерация текста запроса
- Упрощение работы со связями таблиц
- Меньше синтаксических ошибок

**Открытие конструктора:**
- Правой кнопкой мыши в поле запроса → "Конструктор запроса"
- Или через меню в консоли запросов

**Важно:** На начальном этапе лучше использовать конструктор запросов для понимания структуры.

---

#### 2.2 Основные элементы конструктора

**Закладки конструктора:**
- **Таблицы и поля** — выбор источников данных и полей
- **Связи** — настройка соединений между таблицами
- **Условия** — фильтрация данных (WHERE)
- **Группировки** — группировка и агрегация данных
- **Параметры** — параметры запроса
- **Псевдонимы** — переименование полей и таблиц

---

### Блок 3: Простой запрос к справочнику (10 минут)

#### 3.1 Выборка контрагентов

**Задача:** Получить список контрагентов из справочника.

**Шаги в конструкторе:**
1. Добавить таблицу "Справочники.Контрагенты"
2. Добавить поле "Ссылка" или "Наименование"
3. Выполнить запрос

**Результат:**
- Список всех контрагентов из справочника

**Важно:** Обычно в запросах используют ссылку, а не наименование, так как ссылка содержит полную информацию об элементе.

---

#### 3.2 Переименование колонок

**Псевдонимы полей:**
- В конструкторе можно задать псевдоним для поля
- Псевдоним отображается как название колонки в результате
- Пример: поле "Ссылка" → псевдоним "Контрагент"

**Настройка:**
- В конструкторе выбрать поле
- В свойствах поля указать псевдоним
- Или использовать конструкцию `Поле КАК Псевдоним`

---

### Блок 4: Работа с табличными частями (25 минут)

#### 4.1 Проблема транспонирования данных

**Задача:** Получить контактную информацию контрагентов в виде плоской таблицы:
- Контрагент | Адрес | Телефон | Email

**Структура данных:**
- Справочник "Контрагенты" имеет табличную часть "КонтактнаяИнформация"
- Табличная часть содержит: ВидКонтакта (перечисление), Значение (строка)
- Виды контактов: Адрес, Телефон, ЭлектроннаяПочта

**Проблема:** Данные хранятся в вертикальном виде (каждая строка — один вид контакта), а нужны в горизонтальном (каждый вид контакта — отдельная колонка).

---

#### 4.2 Первая попытка: добавление табличной части

**Неправильный подход:**
1. Добавить справочник "Контрагенты"
2. Добавить табличную часть "КонтактнаяИнформация"
3. Добавить поле "Значение"

**Результат:**
- Выводится вся контактная информация в одной колонке
- Адрес, телефон и email смешаны вместе
- Невозможно разделить по колонкам

**Проблема:** Нужно фильтровать по виду контакта для каждой колонки отдельно.

---

#### 4.3 Использование условий WHERE

**Попытка через WHERE:**
```sql
ВЫБРАТЬ
    Контрагенты.Ссылка КАК Контрагент,
    КонтактнаяИнформация.Значение КАК Адрес
ИЗ
    Справочники.Контрагенты КАК Контрагенты
        ЛЕВОЕ СОЕДИНЕНИЕ Справочники.Контрагенты.КонтактнаяИнформация КАК КонтактнаяИнформация
        ПО КонтактнаяИнформация.Ссылка = Контрагенты.Ссылка
ГДЕ
    КонтактнаяИнформация.ВидКонтакта = &ВидКонтактаАдрес
```

**Проблема:**
- Условие в WHERE превращает левое соединение в полное соединение
- Выводятся только контрагенты, у которых есть адрес
- Контрагенты без адреса не попадают в результат

**Важно:** Условия по полям правой таблицы в WHERE превращают левое соединение в полное.

---

#### 4.4 Правильное решение: условие в соединении

**Правильный подход:**
- Условие фильтрации по виду контакта должно быть в самом соединении, а не в WHERE
- Это позволяет сохранить левое соединение

**В конструкторе:**
1. Добавить таблицу "Контрагенты"
2. Добавить табличную часть "КонтактнаяИнформация" с левым соединением
3. В закладке "Связи" добавить условие: `КонтактнаяИнформация.ВидКонтакта = &ВидКонтактаАдрес`
4. Добавить поле "Значение" с псевдонимом "Адрес"

**Результат:**
- Все контрагенты выводятся
- У контрагентов с адресом — значение адреса
- У контрагентов без адреса — NULL

---

#### 4.5 Добавление телефона и email

**Задача:** Добавить еще две колонки — телефон и email.

**Решение:** Создать еще два левых соединения с той же табличной частью, но с разными условиями.

**Шаги:**
1. Добавить еще раз табличную часть "КонтактнаяИнформация"
2. Переименовать таблицы для различия:
   - "КонтрагентыКонтактыАдрес"
   - "КонтрагентыКонтактыТелефон"
   - "КонтрагентыКонтактыEmail"
3. В каждом соединении указать соответствующее условие:
   - Адрес: `ВидКонтакта = &ВидКонтактаАдрес`
   - Телефон: `ВидКонтакта = &ВидКонтактаТелефон`
   - Email: `ВидКонтакта = &ВидКонтактаЭлектроннаяПочта`
4. Добавить поле "Значение" из каждого соединения с соответствующим псевдонимом

**Важно:** Каждое соединение должно быть на том же уровне, что и первое соединение (оба от контрагентов).

---

#### 4.6 Использование функции Значение()

**Проблема:** При каждом выполнении запроса нужно заполнять параметры.

**Решение:** Использовать функцию `Значение()` для фиксации значений перечислений прямо в запросе.

**Синтаксис:**
```sql
ВЫБРАТЬ
    Контрагенты.Ссылка КАК Контрагент,
    КонтактыАдрес.Значение КАК Адрес
ИЗ
    Справочники.Контрагенты КАК Контрагенты
        ЛЕВОЕ СОЕДИНЕНИЕ Справочники.Контрагенты.КонтактнаяИнформация КАК КонтактыАдрес
        ПО КонтактыАдрес.Ссылка = Контрагенты.Ссылка
            И КонтактыАдрес.ВидКонтакта = ЗНАЧЕНИЕ(Перечисление.ВидыКонтактов.Адрес)
```

**В конструкторе:**
- В условии соединения вместо параметра использовать функцию `Значение()`
- Выбрать перечисление и нужное значение

**Преимущества:**
- Не нужно заполнять параметры при выполнении
- Значения зафиксированы в запросе
- Упрощает использование запроса

**Важно:** Функция `Значение()` работает с перечислениями, строками и другими типами данных.

---

### Блок 5: Работа с отчетами и компоновкой данных (20 минут)

#### 5.1 Проблемы с отчетами

**Типичные проблемы:**
- Отчет не формируется
- Ошибки в коде модуля проведения документа
- Неправильная структура запроса
- Не определены ресурсы в компоновке данных

**Пример:** Отчет "Статистика по дням" не работает.

---

#### 5.2 Исправление модуля проведения документа

**Проблема:** Переменная не определена в модуле проведения.

**Решение:**
- Использовать проверку заполнения в свойствах реквизита документа
- Установить "Выдавать ошибку" при незаполненном обязательном поле
- Убрать ручную проверку из кода

**Пример:**
```1c
// Неправильно:
Если ПустаяДата(Объект.ДатаВъезда) Тогда
    ВызватьИсключение "Не заполнена дата въезда";
КонецЕсли;

// Правильно:
// В свойствах реквизита "ДатаВъезда" установить:
// - Обязательность: Да
// - Проверка заполнения: Выдавать ошибку
```

---

#### 5.3 Настройка движений документа

**Проблема:** Движения не создаются при проведении.

**Решение:**
1. В модуле проведения документа вызвать конструктор движений
2. Добавить движение в регистр накопления
3. Указать режим движения (Приход/Расход)
4. Заполнить измерения и ресурсы

**Пример:**
```1c
Процедура Проведение(Отказ)
    
    Движения.КоличествоМашинВДень.Записать();
    
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ)
    
    Для Каждого СтрокаДвижения Из Движения.КоличествоМашинВДень Цикл
        СтрокаДвижения.Период = Объект.ДатаВъезда;
        СтрокаДвижения.КоличествоМашин = 1;
        СтрокаДвижения.ВидДвижения = ВидДвиженияНакопления.Приход;
    КонецЦикла;
    
КонецПроцедуры
```

**Важно:** Период движения должен соответствовать периоду регистратора (дата документа).

---

#### 5.4 Настройка компоновки данных в отчете

**Проблема:** Отчет не формируется, хотя запрос правильный.

**Решение:**
1. В макете компоновки данных добавить набор данных (запрос)
2. Определить ресурсы (поля для суммирования)
3. Добавить группировки (период, измерения)
4. Добавить отбираемые поля

**Шаги:**
1. Открыть макет компоновки данных отчета
2. В наборе данных выбрать запрос
3. В ресурсах добавить поле с агрегацией (Сумма, Количество и т.д.)
4. В группировках добавить период
5. В отбираемых полях добавить нужные поля

---

### Блок 6: Использование виртуальных таблиц (15 минут)

#### 6.1 Проблема производительности

**Проблема:** Использование всей таблицы движений регистра для построения отчета.

**Пример неправильного запроса:**
```sql
ВЫБРАТЬ
    Движения.Дата КАК Дата,
    СУММА(Движения.Количество) КАК КоличествоМашин
ИЗ
    РегистрНакопления.КоличествоМашинВДень.Движения КАК Движения
СГРУППИРОВАТЬ ПО
    Движения.Дата
```

**Проблемы:**
- Выбираются все движения за всю историю
- При большом объеме данных запрос работает медленно
- Неэффективное использование ресурсов

---

#### 6.2 Виртуальные таблицы оборотов

**Решение:** Использовать виртуальную таблицу "Обороты" вместо таблицы "Движения".

**Преимущества:**
- Система уже сгруппировала данные по периодам
- Можно указать период выборки
- Быстрее работает при больших объемах данных

**Синтаксис:**
```sql
ВЫБРАТЬ
    Обороты.Период КАК Период,
    Обороты.КоличествоМашинОборот КАК КоличествоМашин
ИЗ
    РегистрНакопления.КоличествоМашинВДень.Обороты(
        &ДатаНачала,
        &ДатаОкончания,
        День,
        ) КАК Обороты
```

**Параметры виртуальной таблицы:**
- Дата начала периода
- Дата окончания периода
- Периодичность (День, Месяц, Квартал, Год)

**Важно:** Для оборотных регистров не будет прихода и расхода, только оборот.

---

#### 6.3 Периодичность в виртуальных таблицах

**Проблема:** Использование даты как измерения в запросе.

**Решение:** Использовать периодичность в параметрах виртуальной таблицы.

**Преимущества:**
- Система автоматически группирует данные по периоду
- Не нужно делать группировку вручную
- Можно использовать период как поле в запросе

**Пример:**
```sql
ВЫБРАТЬ
    Обороты.Период КАК Период,
    Обороты.КоличествоМашинОборот КАК КоличествоМашин
ИЗ
    РегистрНакопления.КоличествоМашинВДень.Обороты(
        &ДатаНачала,
        &ДатаОкончания,
        День
        ) КАК Обороты
```

**Важно:** Дата не должна быть измерением регистра, если используется периодичность в виртуальной таблице.

---

#### 6.4 Параметры периода в компоновке данных

**Настройка параметров:**
1. В компоновке данных добавить параметры "НачалоПериода" и "КонецПериода"
2. Использовать стандартный период
3. Настроить значения по умолчанию (текущий месяц)

**Выражения для параметров:**
- Начало периода: `ПЕРИОД(НачалоПериода)`
- Конец периода: `ПЕРИОД(КонецПериода)`

**Настройка по умолчанию:**
- В настройках отчета указать период по умолчанию
- Пользователь может изменить период при формировании отчета

---

### Блок 7: Типичные ошибки при составлении запросов (10 минут)

#### 7.1 Неправильный подход: множественные запросы с объединением

**Неправильный способ:**
1. Создать запрос для адреса с условием WHERE
2. Создать запрос для телефона с условием WHERE
3. Создать запрос для email с условием WHERE
4. Объединить запросы через UNION
5. Добавить NULL в недостающие колонки
6. Записать во временную таблицу
7. Сгруппировать по контрагенту с MAX

**Проблемы:**
- Очень большой текст запроса
- Плохая читаемость
- Сложность модификации
- Нужно помнить порядок полей в объединении
- Дополнительная группировка замедляет выполнение

**Пример:**
```sql
ВЫБРАТЬ
    Контрагенты.Ссылка КАК Контрагент,
    Контакты.Значение КАК Адрес,
    NULL КАК Телефон,
    NULL КАК Email
ИЗ
    Справочники.Контрагенты КАК Контрагенты
        ЛЕВОЕ СОЕДИНЕНИЕ Справочники.Контрагенты.КонтактнаяИнформация КАК Контакты
        ПО Контакты.Ссылка = Контрагенты.Ссылка
ГДЕ
    Контакты.ВидКонтакта = &ВидКонтактаАдрес

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
    Контрагенты.Ссылка,
    NULL,
    Контакты.Значение,
    NULL
ИЗ
    ...
ГДЕ
    Контакты.ВидКонтакта = &ВидКонтактаТелефон

-- И так далее...
```

**Важно:** Такой подход не рекомендуется. Лучше использовать несколько левых соединений.

---

#### 7.2 Правильный подход: множественные левые соединения

**Правильный способ:**
- Использовать несколько левых соединений с одной табличной частью
- Каждое соединение с условием по виду контакта
- Простой и понятный запрос

**Преимущества:**
- Компактный текст запроса
- Хорошая читаемость
- Легко модифицировать
- Эффективное выполнение

---

## Резюме семинара (5 минут)

### Ключевые моменты:

1. **Конструктор запросов:**
   - Визуальный инструмент для создания запросов
   - Упрощает работу со связями таблиц
   - Автоматически генерирует текст запроса

2. **Транспонирование данных:**
   - Использовать несколько левых соединений с одной табличной частью
   - Условия фильтрации указывать в соединении, а не в WHERE
   - Каждое соединение для отдельной колонки

3. **Условия в соединениях:**
   - Условия по полям правой таблицы в WHERE превращают левое соединение в полное
   - Условия должны быть в самом соединении
   - Это позволяет сохранить все записи из левой таблицы

4. **Функция Значение():**
   - Позволяет фиксировать значения перечислений в запросе
   - Не нужно заполнять параметры при выполнении
   - Упрощает использование запроса

5. **Виртуальные таблицы:**
   - Использовать виртуальные таблицы оборотов вместо таблицы движений
   - Указывать период выборки для производительности
   - Использовать периодичность вместо группировки по дате

6. **Компоновка данных:**
   - Определять ресурсы для агрегации
   - Настраивать группировки и отбираемые поля
   - Использовать параметры периода

7. **Типичные ошибки:**
   - Множественные запросы с UNION вместо левых соединений
   - Условия в WHERE вместо условий в соединении
   - Использование всей таблицы движений без ограничения периода

---

### Практические рекомендации:

1. **При работе с запросами:**
   - Начинать с конструктора запросов
   - Проверять запросы в консоли запросов
   - Использовать левые соединения для сохранения всех записей

2. **При транспонировании данных:**
   - Использовать несколько левых соединений
   - Условия фильтрации в соединении
   - Переименовывать таблицы для различия

3. **При работе с регистрами:**
   - Использовать виртуальные таблицы оборотов
   - Ограничивать период выборки
   - Использовать периодичность вместо группировки

4. **При создании отчетов:**
   - Проверять модуль проведения документа
   - Настраивать компоновку данных
   - Определять ресурсы и группировки

5. **При отладке:**
   - Использовать консоль запросов для тестирования
   - Проверять структуру данных перед запросом
   - Тестировать на небольшом объеме данных

---

### Вопросы для самопроверки:

1. В чем преимущества конструктора запросов?
2. Как транспонировать данные из табличной части в плоскую таблицу?
3. Почему условие в WHERE превращает левое соединение в полное?
4. Где нужно указывать условия фильтрации при левом соединении?
5. Зачем использовать функцию Значение() в запросах?
6. В чем разница между таблицей движений и виртуальной таблицей оборотов?
7. Почему нужно ограничивать период выборки в запросах к регистрам?
8. Что такое периодичность в виртуальных таблицах?
9. Какие проблемы возникают при использовании UNION для транспонирования данных?
10. Как правильно настроить компоновку данных в отчете?

---

## Практическое задание:

1. Создайте запрос для получения контактной информации контрагентов в виде плоской таблицы (Контрагент | Адрес | Телефон | Email)
2. Используйте конструктор запросов для создания запроса
3. Примените функцию Значение() для фиксации значений перечислений
4. Создайте отчет с использованием виртуальной таблицы оборотов
5. Настройте компоновку данных с параметрами периода
6. Исправьте ошибки в модуле проведения документа
7. Настройте движения документа в регистр накопления

---

**До встречи на следующем семинаре! Вопросы?** 🎓

