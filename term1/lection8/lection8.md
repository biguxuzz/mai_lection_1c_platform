# Лекция 8: Транзакции и блокировки в 1С:Предприятие

**Продолжительность:** 70 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель лекции
Познакомить студентов с механизмами транзакций и блокировок в платформе 1С:Предприятие, их назначением, типами и практическим применением при разработке многопользовательских систем.

## Необходимые предзнания
- Основы работы с платформой 1С:Предприятие
- Работа с документами и регистрами накопления
- Понимание механизма проведения документов
- Базовые знания о многопользовательской работе с базой данных

---

## План лекции (70 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины обсуждения практических примеров и вопросов студентов.

### Блок 1: Введение в транзакции (10 минут)

#### 1.1 Неявные и явные транзакции (5 минут)

В системе 1С:Предприятие существуют **неявные транзакции**, которые автоматически открываются при выполнении определенных операций:
- При записи объектов
- При проведении документов
- При работе с регистрами

**Неявные транзакции** происходят автоматически, без явного указания программиста.

**Явные транзакции** — это транзакции, которые программист организует самостоятельно для обеспечения целостности сложных операций.

**Важно:** В большинстве случаев неявных транзакций достаточно, но для сложных операций может потребоваться явное управление транзакциями.

---

#### 1.2 Назначение транзакций (5 минут)

Транзакции необходимы для решения двух противоречащих задач в многопользовательской СУБД:

1. **Обеспечение параллельности работы** — множество пользователей должны работать одновременно
2. **Сохранение целостности данных** — данные должны оставаться корректными при параллельной работе

**Основные функции транзакций:**

1. **Обеспечение консистентности данных:**
   - Операции с несколькими объектами должны выполниться либо все вместе, либо не выполниться вовсе
   - Транзакция может откатиться целиком или примениться целиком
   - Это обеспечивает целостность данных

2. **Обеспечение изолированности данных:**
   - При работе с объектом в транзакции необходимо предотвратить его изменение другими пользователями
   - Для этого используются блокировки

---

### Блок 2: Типы блокировок (15 минут)

#### 2.1 Разделенная блокировка (5 минут)

**Разделенная блокировка** используется, когда нужно **прочитать** данные и предотвратить их изменение другими транзакциями.

**Применение:**
- Когда в транзакции нужно работать с объектом, но не изменять его
- Объект блокируется для чтения, другие транзакции могут читать, но не могут изменять

**Пример:** Проверка остатков товара перед реализацией.

---

#### 2.2 Эксклюзивная блокировка (5 минут)

**Эксклюзивная блокировка** используется, когда нужно **изменить и записать** объект.

**Применение:**
- Когда в транзакции намерены изменить объект и записать его
- Объект блокируется для записи, другие транзакции не могут ни читать, ни изменять

**Пример:** Проведение документа, который изменяет остатки товаров.

---

#### 2.3 Update-транзакция (5 минут)

**Update-транзакция** — это команда к СУБД о том, какие объекты должны быть заблокированы.

**Особенности:**
- Это не режим управляемых блокировок платформы 1С
- Реализована на уровне СУБД
- Встречается в запросах с конструкцией `FOR UPDATE`

**Ограничения:**
- Достаточно редкая конструкция в современных конфигурациях
- Предпочтительнее работать с менеджером блокировок платформы 1С
- Чем выше уровень работы с блокировками, тем проще управление

**Проблемы:**
- На блокировки СУБД можно повлиять только косвенно
- Отслеживание через технологический журнал
- Программисту сложно осознанно влиять на блокировки на уровне СУБД

**Рекомендация:** Использовать управляемые блокировки платформы 1С вместо Update-транзакций.

---

### Блок 3: Практическая задача — документ "Перемещение товаров" (15 минут)

#### 3.1 Создание документа (5 минут)

**Проблема:** В конфигурации отсутствует документ для перемещения товаров между складами.

**Решение:** Создать документ "Перемещение товаров".

**Структура документа:**
- **Реквизиты:**
  - Склад отправитель
  - Склад получатель
- **Табличная часть "Товары":**
  - Товар
  - Количество
  - Сумма (опционально)

**Настройка прав доступа:**
- Роль "Закупки": редактирование, добавление, отмена, проведение, интерактивная пометка на удаление
- Роль "Продажи": чтение, внешнее чтение

**Движения:**
- Регистр накопления "Товары на складах":
  - Расход со склада отправителя
  - Приход на склад получателя

---

#### 3.2 Контроль остатков (10 минут)

**Задача:** Реализовать контроль остатков для документа "Перемещение товаров", аналогично документу "Реализация товаров".

**Проблема:** Код контроля остатков дублируется в разных документах.

**Решение:** Выделить контроль остатков в общий модуль.

**Создание общего модуля "КонтрольОстатков":**
- Тип: серверный
- Экспортная функция для проверки остатков

**Параметры функции:**
- Табличная часть товаров
- Склад
- Дата (опционально, для оперативных остатков не требуется)

**Возвращаемое значение:**
- Булево значение (истина — остатки в порядке, ложь — есть отрицательные остатки)
- Текст ошибки (при наличии проблем)

---

### Блок 4: Временное хранилище (10 минут)

#### 4.1 Назначение временного хранилища (5 минут)

**Временное хранилище** предназначено для передачи данных между контекстами:
- Между клиентским и серверным контекстами
- Для передачи больших объемов данных без копирования

**Принцип работы:**
- Данные помещаются во временное хранилище
- Возвращается адрес (уникальный идентификатор) во временном хранилище
- Адрес передается в другой контекст
- Данные извлекаются по адресу

**Преимущества:**
- Эффективная передача данных
- Не требуется копирование больших объектов
- Автоматическая очистка при завершении сеанса

---

#### 4.2 Использование в контроле остатков (5 минут)

**Задача:** Передать табличную часть товаров из документа в функцию контроля остатков.

**Решение:** Использовать временное хранилище.

**Алгоритм:**
1. Выгрузить табличную часть товаров во временное хранилище
2. Получить адрес во временном хранилище
3. Передать адрес в функцию контроля остатков
4. В функции извлечь данные по адресу
5. Выполнить проверку остатков
6. Очистить временное хранилище после использования

**Важно:** 
- Использовать метод `ВыгрузитьСтроки()` для выгрузки данных табличной части
- Метод `ВыгрузитьКолонки()` создает пустую таблицу с заданным типом колонок
- После использования рекомендуется очищать временное хранилище

---

### Блок 5: Особенности контроля остатков (10 минут)

#### 5.1 Проблема с датой при контроле остатков (5 минут)

**Ошибка:** Передача даты документа в функцию контроля остатков.

**Проблема:**
- Регистр остатков работает с оперативными остатками
- При передаче даты документа запрос срабатывает до момента записи движений
- Изменение остатков происходит после записи документа
- Контроль выполняется на неправильную дату

**Решение:**
- Для оперативных остатков дату передавать не нужно
- Контроль выполняется на актуальную дату (конец всех операций)
- Дата в параметрах функции должна быть опциональной

**Важно:** Контроль остатков должен выполняться после записи движений документа.

---

#### 5.2 Последовательность записи движений в регистры (5 минут)

**Вопрос:** Можно ли повлиять на порядок записи движений в регистры?

**Ответ:** Нет, нельзя повлиять на порядок записи движений в регистры.

**Особенности:**
- Порядок записи не зависит от порядка регистров в метаданных
- Порядок записи не зависит от порядка добавления регистров в код
- Платформа определяет порядок записи самостоятельно

**Практическое значение:**
- Нельзя полагаться на порядок записи при разработке логики
- Контроль остатков должен выполняться после записи всех движений
- Использовать метод `ЗаписатьДвижения()` с параметром `Записывать = Истина`

---

### Блок 6: Управляемые блокировки данных (10 минут)

#### 6.1 Назначение блокировок (5 минут)

**Проблема:** При параллельной работе нескольких пользователей может возникнуть конфликт при работе с одними и теми же данными.

**Пример конфликта:**
- Пользователь 1 проводит документ "Перемещение товаров" со склада А
- Пользователь 2 одновременно проводит документ "Реализация товаров" с того же склада А
- Оба документа работают с одними и теми же остатками
- Возможна ситуация, когда оба документа пройдут, но остатки станут отрицательными

**Решение:** Использовать управляемые блокировки данных.

**Применение:**
- При сложных вычислениях себестоимости
- При организации партионного учета (например, LIFO)
- Когда нужно обеспечить атомарность операции с несколькими объектами

---

#### 6.2 Реализация блокировок (5 минут)

**Механизм блокировки данных:**

```1c
// Блокировка регистра накопления
БлокировкаДанных = Новый БлокировкаДанных;
БлокировкаДанных.Заблокировать(
    РегистрыНакопления.ТоварыНаСкладах,
    Новый Структура("Склад", СкладОтправитель),
    РежимБлокировкиДанных.Исключительный,
    ИсточникБлокировки.Товары
);
```

**Параметры блокировки:**
- **Регистр накопления** — что блокируем
- **Измерения** — по каким разрезам блокируем (склад, товар)
- **Режим блокировки** — исключительный (для записи) или разделенный (для чтения)
- **Источник блокировки** — идентификатор источника блокировки

**Важно:**
- Блокировка устанавливается внутри неявной транзакции (при проведении документа)
- Тайм-аут блокировки по умолчанию — 20 секунд
- При превышении тайм-аута возникает ошибка: "Превышено максимальное время ожидания представления блокировки"

---

### Блок 7: Практический пример — предотвращение конфликтов (10 минут)

#### 7.1 Симуляция конфликта (5 минут)

**Сценарий:**
1. Создать документ "Перемещение товаров" с блокировкой на 35 секунд
2. В первой сессии провести документ "Перемещение товаров" (блокировка активна)
3. Во второй сессии попытаться провести документ "Реализация товаров" с того же склада
4. Наблюдать конфликт блокировок

**Результат:**
- Документ "Реализация товаров" не может быть проведен, пока активна блокировка
- Возникает ошибка о превышении времени ожидания блокировки
- Документ с другим складом или другим товаром проходит без проблем

**Вывод:** Блокировка работает только для конкретных измерений (склад, товар).

---

#### 7.2 Применение блокировок для LIFO (5 минут)

**LIFO (Last In, First Out)** — метод учета партий товара "последний пришел, первый ушел".

**Проблема без блокировок:**
- При проведении реализации выбирается последняя партия
- Пока идет обработка партии, другой пользователь может провести документ с тем же товаром
- Учет LIFO может измениться некорректно

**Решение:**
- Установить блокировку на товар и склад перед выборкой партии
- Удерживать блокировку до завершения операции
- Это обеспечит корректность учета LIFO

**Важно:** Блокировки следует использовать только когда это действительно необходимо, так как они снижают параллельность работы системы.

---

## Резюме лекции (5 минут)

### Ключевые моменты:

1. **Транзакции в 1С:**
   - Неявные транзакции открываются автоматически при записи и проведении
   - Явные транзакции организуются программистом для сложных операций
   - Обеспечивают консистентность и изолированность данных

2. **Типы блокировок:**
   - Разделенная блокировка — для чтения данных
   - Эксклюзивная блокировка — для записи данных
   - Update-транзакция — команда к СУБД (редко используется)
   - Предпочтительнее использовать управляемые блокировки платформы 1С

3. **Временное хранилище:**
   - Для передачи данных между контекстами
   - Эффективная передача больших объемов данных
   - Требует очистки после использования

4. **Контроль остатков:**
   - Выделение в общий модуль для переиспользования
   - Для оперативных остатков дата не требуется
   - Контроль выполняется после записи движений

5. **Управляемые блокировки:**
   - Для предотвращения конфликтов при параллельной работе
   - Блокировка по измерениям регистра
   - Тайм-аут блокировки — 20 секунд по умолчанию
   - Применение: сложные вычисления, партионный учет (LIFO)

---

### Практические рекомендации:

1. **Использование транзакций:**
   - В большинстве случаев достаточно неявных транзакций
   - Явные транзакции используйте для сложных операций с несколькими объектами
   - Убедитесь, что транзакция либо полностью выполняется, либо полностью откатывается

2. **Работа с блокировками:**
   - Используйте управляемые блокировки платформы 1С
   - Блокируйте только необходимые измерения
   - Учитывайте тайм-аут блокировок (20 секунд)
   - Не используйте блокировки без необходимости (снижают параллельность)

3. **Контроль остатков:**
   - Выделяйте общий код в общий модуль
   - Используйте временное хранилище для передачи табличных частей
   - Контролируйте остатки после записи движений
   - Для оперативных остатков не передавайте дату

4. **Оптимизация:**
   - Очищайте временное хранилище после использования
   - Используйте метод `ВыгрузитьСтроки()` для выгрузки данных
   - Не полагайтесь на порядок записи движений в регистры

---

### Вопросы для самопроверки:

1. В чем разница между неявными и явными транзакциями?
2. Когда использовать разделенную блокировку, а когда эксклюзивную?
3. Почему Update-транзакции редко используются в современных конфигурациях?
4. Для чего предназначено временное хранилище?
5. Почему при контроле оперативных остатков не нужно передавать дату?
6. Можно ли повлиять на порядок записи движений в регистры?
7. Когда следует использовать управляемые блокировки данных?
8. Что происходит при превышении тайм-аута блокировки?
9. Как блокировки помогают при реализации метода LIFO?
10. Почему блокировки снижают параллельность работы системы?

---

## Следующая лекция:

**Лекция 9: Работа с хранилищем в 1С:Предприятие**

**План лекции:**
1. Введение в хранилище
   - Назначение хранилища
2. Временное хранилище для передачи данных
   - Создание формы подбора
   - Организация списка выбора
   - Передача данных через временное хранилище
3. Хранилище значений для картинок
   - Добавление реквизита для картинки
   - Загрузка картинки
   - Сохранение картинки в объект

**До встречи на следующей лекции! Вопросы?** 🎓

