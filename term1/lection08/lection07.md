# Лекция 7: Веб-сервисы и HTTP-сервисы в 1С:Предприятие

**Продолжительность:** ~45 минут  
**Целевая аудитория:** Студенты 2 курса, Факультет информационных технологий  
**Курс:** Разработка на платформе 1С:Предприятие

## Цель лекции
Познакомить студентов с технологиями веб-сервисов и HTTP-сервисов в платформе 1С:Предприятие, их различиями, практическим применением и созданием интеграционных решений.

## Необходимые предзнания
- Основы работы с платформой 1С:Предприятие
- Понятие конфигурации и метаданных
- Работа со справочниками и регистрами сведений
- Базовые навыки программирования на встроенном языке 1С

---

## План лекции (~45 минут)

> **Примечание:** Время может варьироваться в зависимости от глубины обсуждения практических примеров и вопросов студентов.

### Блок 1: Введение в веб-сервисы и HTTP-сервисы (5 минут)

#### 1.1 Общие технологии (2 минуты)

**Общие характеристики:**
- Оба типа сервисов используют протокол HTTP/HTTPS
- Работают через веб-сервер
- База данных публикуется через веб-сервер
- Используются для интеграции с внешними системами

**Техническая основа:**
- Протокол: HTTP или HTTPS
- Публикация через веб-сервер
- Доступ к базе данных через веб-интерфейс

---

#### 1.2 Различия между веб-сервисами и HTTP-сервисами (3 минуты)

**Веб-сервисы:**
- Описывают событийную модель, работают с командами
- Параметры и данные хранятся в оперативной памяти
- Используются для связи разных компонентов системы
- Подходят для создания API для внешних пользователей и систем
- Поддерживают версионирование (можно поддерживать несколько версий API одновременно)
- Создают WSDL-файл (Web Services Description Language) с описанием API
- WSDL позволяет другим системам автоматически подключаться к сервису
- Валидация параметров происходит на стороне клиента
- Не требуется валидация данных на стороне сервера

**HTTP-сервисы:**
- Более легковесные решения
- Используются для быстрых интеграций
- Разработчик может доработать интеграцию как со стороны клиента, так и со стороны сервера
- Можно входящие файлы сразу записывать на диск
- Быстрая разработка
- **Минусы:** При создании массового сервиса нужно продумывать все обработчики ошибок

**Когда использовать:**
- **Веб-сервисы:** Для публичных API, долгосрочных интеграций, когда нужна версионируемость
- **HTTP-сервисы:** Для быстрых интеграций между своими системами, переноса данных из одной базы в другую

---

### Блок 2: Создание веб-сервиса (15 минут)

#### 2.1 Создание структуры веб-сервиса (5 минут)

**Пример:** Создание веб-сервиса для установки цен товаров

**Шаги создания:**

1. **Создание операции веб-сервиса:**
   - Название: "УстановкаЦенТоваров"
   - Возвращаемое значение: `Boolean` (истина при успешной установке)
   - Синоним: "Установка цен товаров"

2. **Параметры операции:**
   - **Товар** (входной, обязательный) — строка
   - **Цена** (входной, обязательный) — число (float)
   - **Сообщение** (выходной, необязательный) — строка для возврата сообщений об ошибках

3. **Пространство имен:**
   - Формат: `urn:1C:8:<Разработчик>:<НазваниеСистемы>:<Блок>`
   - Пример: `urn:1C:8:Developer:SalesManagement:Price`
   - Пакеты XDTO не требуются для простых объектов

4. **Имя веб-сервиса:**
   - Пример: "Price"

**Преимущества веб-сервисов:**
- Валидация параметров на стороне клиента
- Не нужно валидировать данные на сервере
- Автоматическое создание WSDL-файла

---

#### 2.2 Публикация веб-сервиса (5 минут)

**Требования для публикации:**

1. **Установка веб-сервера:**
   - Для Linux: `sudo apt install apache2` (или другой веб-сервер)
   - Веб-сервер должен быть установлен и настроен

2. **Публикация через конфигуратор:**
   - Зайти в конфигуратор от пользователя с правами администратора
   - Администрирование → Публикация на веб-сервере
   - **Важно:** Контролировать версию Apache (рекомендуется 2.4), иначе будет использована неправильная библиотека

3. **Настройки публикации:**
   - Указать расположение публикации (обычно `/var/www/http/` + имя базы для Debian-систем)
   - Отметить галочку "Публиковать веб-сервисы"
   - Дополнительно можно опубликовать веб-приложение для доступа через браузер

4. **Проверка публикации:**
   - Зайти через браузер в приложение (проверка общей публикации)
   - Доступ к WSDL: `http://<адрес>/<база>/ws/<имя_сервиса>?wsdl`
   - Пример: `http://localhost/base/ws/price1s-vs?wsdl`

---

#### 2.3 Настройка ролей и пользователей (5 минут)

**Создание роли для внешнего подключения:**

1. **Создание роли:**
   - Название: например, "ВнешнееПодключение"
   - **Важно:** Оставить только "Внешнее соединение" (убрать все виды клиентов)
   - Выдать права на выполнение конкретного метода веб-сервиса (например, `setPrice`)

2. **Создание пользователя:**
   - Имя: например, "remote-user"
   - Пароль: задать пароль
   - Выдать права: "Внешнее подключение"
   - Назначить созданную роль

**Безопасность:**
- Пользователь для веб-сервиса не должен иметь доступ через обычные клиенты
- Минимальные права доступа (только необходимые операции)
- На больших проектах рекомендуется правильно расставлять права на все используемые объекты

---

### Блок 3: Создание клиента для веб-сервиса (10 минут)

#### 3.1 Создание обработки-клиента (5 минут)

**Структура обработки:**

1. **Реквизиты формы:**
   - Товар (строка)
   - Цена (число)

2. **Команда на сервере:**
   - Название: "УстановитьЦену"
   - Обработчик будет обращаться к веб-сервису

3. **Создание WS-прокси:**
   - Использовать WSDL для получения описания сервиса
   - Указать URI пространства имен (из настроек веб-сервиса)
   - Указать имя сервиса и имя порта (из WSDL)
   - Настроить соединение с указанием пользователя и пароля

**Пример кода создания WS-прокси:**
```1c
// Получение описания веб-сервиса через WSDL
WSСсылки = Новый WSСсылки;
WSПрокси = WSСсылки.ПолучитьWSПрокси("http://localhost/base/ws/price1s-vs?wsdl");

// Настройка соединения
Соединение = Новый WSСоединениеHTTP;
Соединение.Пользователь = "remote-user";
Соединение.Пароль = "пароль";
WSПрокси.УстановитьСоединение(Соединение);
```

---

#### 3.2 Вызов метода веб-сервиса (5 минут)

**Обработчик команды:**

1. **Подготовка переменных:**
   - Переменная для сообщения (выходной параметр)
   - Переменная для результата

2. **Вызов метода:**
   - Передать все параметры (товар, цена, сообщение)
   - Получить результат (Boolean)

3. **Обработка результата:**
   - Если результат = Истина — показать сообщение об успехе
   - Если результат = Ложь — показать сообщение об ошибке

**Пример кода:**
```1c
Сообщение = "";
Результат = WSПрокси.УстановкаЦенТоваров(Товар, Цена, Сообщение);

Если Результат Тогда
    Сообщить("Цена успешно установлена");
Иначе
    Сообщить("Ошибка: " + Сообщение);
КонецЕсли;
```

---

### Блок 4: Реализация логики веб-сервиса (10 минут)

#### 4.1 Поиск товара (3 минуты)

**Логика поиска:**

1. **Поиск по наименованию:**
   - Использовать метод `НайтиПоНаименованию()` справочника
   - **Важно:** Установить параметр "Точное соответствие" = Истина
   - Без точного соответствия может быть найдена группа вместо элемента

2. **Проверка результата:**
   - Если товар не найден — вернуть Ложь и сообщение об ошибке
   - Если товар найден — продолжить выполнение

**Пример кода:**
```1c
ТоварОбъект = Справочники.Товары.НайтиПоНаименованию(Товар, Истина);

Если ТоварОбъект.Пустая() Тогда
    Сообщение = "Товар '" + Товар + "' не найден";
    Возврат Ложь;
КонецЕсли;
```

---

#### 4.2 Запись в регистр сведений (5 минут)

**Работа с регистром сведений:**

1. **Создание набора записей:**
   - Регистр: "ЦеныТоваров" (периодический, независимый)
   - Измерение: Товар
   - Ресурс: Цена

2. **Важно — установка отбора:**
   - При работе с независимыми регистрами сведений запись без отбора очистит весь регистр!
   - Обязательно установить отбор по товару
   - Установить параметр "Замещать" = Истина (для обновления существующих записей)

3. **Добавление записи:**
   - Установить ссылку на товар
   - Установить цену
   - Установить период (для периодического регистра)

**Пример кода:**
```1c
НаборЗаписей = РегистрыСведений.ЦеныТоваров.СоздатьНаборЗаписей();
НаборЗаписей.Отбор.Товар.Установить(ТоварОбъект);
НаборЗаписей.Замещать = Истина;

НоваяЗапись = НаборЗаписей.Добавить();
НоваяЗапись.Товар = ТоварОбъект;
НоваяЗапись.Цена = Цена;
НоваяЗапись.Период = ТекущаяДата();

НаборЗаписей.Записать();
```

---

#### 4.3 Обработка ошибок (2 минуты)

**Обработка исключений:**

1. **Использование блока "Попытка-Исключение":**
   - Обернуть запись в регистр в блок попытки
   - При ошибке сформировать сообщение и вернуть Ложь
   - При успехе вернуть Истину

2. **Получение сообщения об ошибке:**
   - Использовать `ОписаниеОшибки()` для получения текста ошибки
   - Записать в выходной параметр "Сообщение"

**Пример кода:**
```1c
Попытка
    НаборЗаписей.Записать();
    Результат = Истина;
Исключение
    Сообщение = "Не удалось записать цену: " + ОписаниеОшибки();
    Результат = Ложь;
КонецПопытки;
```

---

### Блок 5: Права доступа и привилегированный режим (5 минут)

#### 5.1 Проблема прав доступа

**Типичная ситуация:**
- Пользователь для веб-сервиса имеет права только на выполнение метода
- При обращении к справочникам и регистрам возникает ошибка "Отказ в доступе"
- Ошибка фиксируется в журнале регистрации

**Решение 1: Правильная настройка прав:**
- Выдать пользователю права на чтение справочника "Товары"
- Выдать права на запись в регистр сведений "ЦеныТоваров"
- Рекомендуемый подход для больших проектов

**Решение 2: Привилегированный режим:**
- Установить `ПривилегированныйРежим = Истина` в методе веб-сервиса
- Код будет выполняться с повышенными правами
- **Важно:** На больших проектах это обычно обнаруживается при проверке кода как замечание
- Рекомендуется использовать только для быстрых прототипов

**Пример:**
```1c
Процедура УстановкаЦенТоваров(Товар, Цена, Сообщение) Экспорт
    ПривилегированныйРежим = Истина;
    // ... код метода ...
КонецПроцедуры
```

---

### Блок 6: Особенности работы с регистрами сведений (5 минут)

#### 6.1 Замещение записей

**Параметр "Замещать":**

- **Замещать = Истина:**
  - Если запись с такими же измерениями уже существует — она будет обновлена
  - Повторные вызовы не создают дубликаты
  - Подходит для обновления существующих данных

- **Замещать = Ложь:**
  - При попытке записать запись с существующими измерениями возникнет ошибка
  - Нужно сначала удалить старую запись
  - Подходит для контроля уникальности

**Пример:**
- При установке цены на товар "Шина" три раза подряд:
  - С замещением: запись обновится один раз (последнее значение)
  - Без замещения: первая запись создастся, последующие вызовы приведут к ошибке

---

## Резюме лекции (5 минут)

### Ключевые моменты:

1. **Веб-сервисы vs HTTP-сервисы:**
   - Веб-сервисы: для публичных API, версионирование, WSDL
   - HTTP-сервисы: для быстрых интеграций, легковесные решения

2. **Создание веб-сервиса:**
   - Определение операции с параметрами
   - Настройка пространства имен
   - Публикация на веб-сервере
   - Создание WSDL-файла

3. **Безопасность:**
   - Создание отдельной роли для внешнего подключения
   - Пользователь только с правами "Внешнее соединение"
   - Минимальные права доступа
   - Привилегированный режим — только для прототипов

4. **Работа с регистрами сведений:**
   - Обязательно устанавливать отбор при работе с независимыми регистрами
   - Параметр "Замещать" для обновления существующих записей
   - Периодические регистры требуют указания периода

5. **Обработка ошибок:**
   - Использование блока "Попытка-Исключение"
   - Возврат сообщений об ошибках через выходные параметры
   - Проверка журнала регистрации при проблемах с правами доступа

---

### Практические рекомендации:

1. **Выбор типа сервиса:**
   - Веб-сервисы — для долгосрочных интеграций и публичных API
   - HTTP-сервисы — для быстрых интеграций между своими системами

2. **Безопасность:**
   - Всегда создавайте отдельную роль для веб-сервисов
   - Минимизируйте права доступа
   - Избегайте привилегированного режима в продакшене

3. **Работа с данными:**
   - Всегда устанавливайте отбор при работе с независимыми регистрами сведений
   - Используйте точное соответствие при поиске по наименованию
   - Обрабатывайте все возможные ошибки

4. **Отладка:**
   - Проверяйте журнал регистрации при ошибках доступа
   - Тестируйте веб-сервис через простую обработку-клиент
   - Проверяйте WSDL для понимания структуры API

---

### Вопросы для самопроверки:

1. В чем основное различие между веб-сервисами и HTTP-сервисами?
2. Что такое WSDL и для чего он используется?
3. Почему важно устанавливать отбор при работе с независимыми регистрами сведений?
4. Что произойдет, если не установить параметр "Точное соответствие" при поиске по наименованию?
5. В чем разница между параметром "Замещать = Истина" и "Замещать = Ложь"?
6. Почему не рекомендуется использовать привилегированный режим в продакшене?
7. Какие права должен иметь пользователь для работы с веб-сервисом?
8. Как проверить, что веб-сервис опубликован корректно?
9. Что нужно сделать, если при вызове веб-сервиса возникает ошибка "Отказ в доступе"?
10. Зачем нужен выходной параметр "Сообщение" в методе веб-сервиса?

---

## Домашнее задание:

**Задание:** В вашу базу данных, которую вы разрабатываете, нужно добавить веб-сервис, релевантный вашей предметной области.

**Примеры:**
- Установка скидок
- Установка цен
- Создание заказов
- Обновление остатков

**Требования:**
1. Создать веб-сервис с минимум одним методом
2. Опубликовать веб-сервис на веб-сервере
3. Создать роль и пользователя для внешнего подключения
4. Создать обработку-клиент для тестирования веб-сервиса
5. Реализовать логику метода (работа с данными)
6. Обработать возможные ошибки

---

## Следующая лекция:

**Лекция 8: Транзакции и блокировки в 1С:Предприятие**

**План лекции:**
1. Введение в транзакции
   - Неявные и явные транзакции
   - Назначение транзакций
2. Типы блокировок
   - Разделенная блокировка
   - Эксклюзивная блокировка
   - Update-транзакция
3. Практическая задача — документ "Перемещение товаров"
   - Создание документа
   - Контроль остатков
4. Временное хранилище
   - Назначение временного хранилища
   - Использование в контроле остатков
5. Особенности контроля остатков
   - Проблема с датой при контроле остатков
   - Последовательность записи движений в регистры
6. Управляемые блокировки данных
   - Назначение блокировок
   - Реализация блокировок
7. Практический пример — предотвращение конфликтов
   - Симуляция конфликта
   - Применение блокировок для LIFO

**До встречи на следующей лекции! Вопросы?** 🎓

